# Product Pulse

Product Pulse is a dependency-free Node.js application using ECMAScript modules.

Run the service with `npm start --prefix app` and its built-in test suite with
`npm test --prefix app`.
