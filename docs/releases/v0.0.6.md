# ZaoFu v0.0.6 Release Notes（Draft）

[English](v0.0.6.en.md)

> 状态：发布草案，尚未打 tag 或发布包。
>
> 草案日期：2026-08-19
>
> 建议发布候选提交：`a7b60944`（`merge: land evolution environment closure`）
>
> 对比基线：v0.0.5 草案候选提交 `bccf76dc`（`merge: improve conversation UX`）
>
> 版本序列说明：v0.0.3、v0.0.4 和 v0.0.5 都仍是未打 tag 的草案，Python 与 Web
> 包元数据仍为 `0.0.2`。正式发布 v0.0.6 前，发布负责人必须处理前序草案并统一 Python、CLI、
> Web 与 lockfile 的版本。本说明不修改版本元数据，也不创建 tag。
>
> 运行要求：Python 3.11+、`uv`、`tmux`；Web 需要 Node.js/npm；真实浏览器验收推荐
> Docker + `mcp/playwright:latest`；真实 Agent 运行需要已认证的 Provider CLI。
>
> 许可证：Apache License 2.0

## v0.0.6 版本

v0.0.6 聚焦 **可控的人机协作收口、可解释的运行观测，以及受证据约束的能力积累**。
它将 Channel/Kanban Agent 中的可见对话、人工决策和 Provider 完整性进一步分层；同时新增
默认关闭的 OTLP/Operations 观测面，并把 Autoresearch 的已验证 Learn 结果接入可比较、可回退的
Evolution 生命周期。

```text
可见对话 / 人工决策
  -> EventLog + sidecar + controlled action
  -> Task / Gate / Delivery 的既有权威边界

已验证的 Learn Run
  -> immutable attempt + environment snapshot
  -> baseline / candidate / independent canary
  -> retained asset 或 revoked
```

这不是让 Web、监控系统或 Agent 获得 Task/Gate 的写权。Channel、Observability 和 Evolution
继续通过事件、artifact 与受控动作与 Kernel 协作，不能成为第二个 canonical state machine。

## 发布亮点

### 1. Channel 与 Kanban Agent 的决策和流式回复更可控

- Channel 将执行状态与人工注意力分为两条只读轴：Agent 仍在回复时，页面会同时呈现正在响应的
  Agent 与待决策项；只有执行范围确实等待 Owner 时才进入 `Waiting for you`。
- `Operator Inbox -> Decisions` 聚合当前 Channel owner question 和 Kanban Agent plan request，
  但只提供回到原始对话的深链。实际回答继续经过原有的 question/plan 受控入口。
- Channel 的可见 Markdown 与机器 `channel_contribution` 合同分离；Kanban Agent 的
  `plan_request`、`action_proposal` 控制块不会先作为 JSON 闪现在聊天中。控制块识别后显示
  Preparing 状态，canonical request 到达后才替换为可操作卡片。
- Provider 输出不完整或缺少必需 typed contract 时，Channel 会执行有界 continuation，而不是把
  不完整回复直接作为讨论结论。完整 raw reply、typed contract 和 artifact 仍保留用于审计与重放。

### 2. Provider 与运行可观测性成为独立、可关闭的运维面

- 新增 provider capability、受管 Claude Code per-turn telemetry、脱敏 Runtime Logs、
  Operations 摘要、低基数 Prometheus `/metrics`、OTLP/HTTP synthetic span exporter 和
  attention 投影。
- OTLP exporter 从 EventLog 的只读投影异步发送，使用有界 batch、cursor、idempotency key、
  backoff 与采样策略。collector 故障只产生可诊断 health/backlog，不阻塞 EventWriter、
  Provider dispatch 或 Workflow。
- Delivery Graph、Run Graph 与 Fanout DAG 继续保留原有数据模型和页面职责；Operations、
  Runtime Logs、Events 分别回答运维健康、进程诊断和业务因果问题。
- 端点、header 和 token 只从环境变量读取；Prompt、回复、tool body、绝对路径和高基数业务 ID
  不会进入 metric label、Web read API 或 exporter payload。

### 3. Autoresearch 到自我进化的证据闭环

- 已验证的 Learn Run 通过 Run Archive 中唯一的 typed deposition 进入 immutable campaign 和
  evolution attempt；普通 Event 文本、聊天投影或未归档回复不能直接被提升为能力。
- baseline/candidate 使用冻结的 evaluator、provider、toolchain、environment、sandbox、network、
  credential-policy 等比较身份。缺 CLI、认证、sealed evaluator、凭据能力或环境漂移会在调用
  Provider 前终止为明确证据，不会被误记为 candidate 质量失败或无限重试。
- 资产需要经过重复 trial、sealed evaluator、独立 canary 和 receipt-bound 状态转换后，才可进入
  `active_retained`；负迁移、冲突或证据不足可被撤销。
- `auto_low_risk` 仅在显式启用时可自动保留 allowlist 中的 `memory_entry`、`runbook`、
  `regression_fixture`。`framework_code`、`workflow_config`、`provider_route` 与
  `tool_capability` 继续保持 proposal-only，必须由对象特定 Owner 批准和 apply。

### 4. 有界的执行策略演进与开发辅助能力

- Run Manager 可针对结构化 provider/capability/context 失败，在预批准的静态 route catalog 内
  为同一 Task Owner 选择一次有界 execution-route switch，并写入绑定 Task、Run、checkpoint 与
  policy digest 的 receipt。它不会动态发明 Provider、模型、权限或工具范围。
- 新增 `zf-find-simplifications` 与 `zf-record-browser-demo` Skills：前者以 consumer/protected
  seam 证据提出简化建议，后者以 Docker Playwright 的可复现帧、GIF 与 provenance 记录浏览器
  工作流。两者均不绕过既有测试、Owner gate 或发布流程。

## 本版本重点能力合同

<!-- ZF-CAPABILITY: channel-to-prd -->
- Activation / 启用: 启动 Web，选择 Project 后在 Channel Group 或 Kanban Agent 发起讨论；仅由 Owner 的受控操作将已确认 PRD 继续转为 Task/Workflow。
- Readback / 回读: 对照 Channel conversation、discussion attention、Decisions deep link、question/plan receipt、EventLog 与 sidecar，确认可见正文、人工输入和 PRD 来源一致。
- Rollback / 回退: 回退 Channel/Kanban 的读投影或禁用相应界面版本；保留 EventLog、Channel state、question/plan receipt 与 sidecar，不编辑聊天展示来伪造完成。
- Authority / 权限边界: Channel 讨论、Kanban Agent、Inbox 与 Web 都是事件/sidecar 的投影或受控动作入口；只有 Owner-confirmed artifact 才能成为 canonical PRD，Channel 不拥有 Workflow dispatch。
- Manual / 文档: [Channel 协作](../manual/15-channel-collaboration.md)
<!-- ZF-CAPABILITY-END -->

<!-- ZF-CAPABILITY: provider-native-operations-observability -->
- Activation / 启用: 仅通过 observability 配置中的环境变量名显式启用 telemetry、metrics、OTLP exporter 或 alerts，并启动 runtime tick；Web 只负责回读。
- Readback / 回读: 在 Operations 查看 Provider capability 与 exporter health，在 Runtime Logs 查看脱敏诊断，在带 token 的 `/metrics` 查看低基数指标，并回到 Delivery/Trace 确认业务事实。
- Rollback / 回退: 在新的 runtime generation 关闭对应 observability block；保留 projection、cursor 和日志供审计，禁止为了恢复面板而修改 Task、Event 或 Delivery。
- Authority / 权限边界: telemetry、logs、metrics、exporter 和 alerts 都是诊断投影，不能派发 Provider、写 canonical store，或决定 Gate/Judge verdict。
- Manual / 文档: [Metrics、Observability 与 Operations](../manual/21-metrics-observability-operations.md)；[Provider Native Telemetry 与 OTLP](../manual/22-provider-native-telemetry.md)
<!-- ZF-CAPABILITY-END -->

<!-- ZF-CAPABILITY: evidence-bound-self-evolution -->
- Activation / 启用: 启用 Autoresearch resident 和显式 `runtime.evolution` policy，并先校验 sealed evaluator root 与凭据环境变量名；推荐从 `evaluate_only` 开始。
- Readback / 回读: 检查 evolution status、EventLog lineage、attempt/trial/canary artifact、环境快照、Run Archive、capability state 及后续 usage outcome。
- Rollback / 回退: 关闭 evolution 或退回 `evaluate_only`，再通过 receipt-bound controlled transition 撤销资产；保留所有比较和 canary 证据。
- Authority / 权限边界: Evolution 是证据与能力生命周期，不能改写产品 Task/Delivery truth；Coordinator 不会直接修改源码、`zf.yaml`、Skills 或普通 Memory。
- Manual / 文档: [自我进化与能力积累](../manual/23-self-evolution-learning.md)
<!-- ZF-CAPABILITY-END -->

## 从 v0.0.5 草案到 v0.0.6 草案

从 `bccf76dc` 到候选提交 `a7b60944`：

- 34 个提交，其中 27 个非 merge 提交；
- 240 个文件变化，新增 32,922 行、删除 928 行；
- 主要涉及 78 个 `src/zf/` 文件、23 个 `web/src/` 文件、27 个完成任务记录、
  Provider/OTLP 与 Evolution 的确定性和真实 Provider E2E 测试、以及中英文操作手册。

这些数字包含测试、任务记录、报告、手册、Skills 和二进制文档资产，不等同于净产品代码量。
发布质量应以受控交互、事件/证据权威、可观察性隔离和真实 Provider 验证为准。

## 升级说明

1. **保持默认关闭**：不配置 `observability` 或 `runtime.evolution` 时，新能力不会自动启动
   collector、exporter、自动采纳或修改现有项目行为。
2. **更新依赖和 Web 资源**：部署更新后执行 `uv sync --extra dev --extra web`、
   `npm --prefix web ci`、`npm --prefix web run build`，然后重启 runtime 与 Web Dashboard。
3. **不手工迁移业务真相**：不要编辑 `events.jsonl`、Task/Feature store、Channel sidecar 或
   `capabilities.json` 来修复页面或调整资产状态。新投影应由运行时和受控命令重建。
4. **先在隔离环境启用观测/进化**：使用 `/tmp/zf-<purpose>-<utc>/`、可控 collector 和最小
   policy canary 验证 environment preflight、脱敏、token gate、backoff、比较与回退后再扩大范围。
5. **保留 Provider 边界**：受管原生 telemetry 当前只对可安全 per-turn 注入的 Claude Code
   headless/stream-json route 宣称支持；Codex 目前只报告 capability/probe 状态，ZaoFu
   synthetic OTLP span 不能被视为 Codex 原生 trace。

## 已知限制与发布阻断项

1. **版本元数据未升级**：Python 与 Web 仍为 `0.0.2`，且 v0.0.3 至 v0.0.5 尚无公开 tag；
   正式发布前必须先解决版本序列和元数据一致性。
2. **Evolution 是 partial capability**：它不是模型训练，也不会默认改写源码、配置、Skills 或
   主分支。高风险资产仍需要 proposal、Owner approval 和受控 apply。
3. **上下文与普通 Memory 不是完整历史重放器**：普通 Memory 的 `max_days` 仍是元数据，
   并非逐项严格 expiry/applicability gate；长期复用应检查 retained asset 的 scope、证据、
   conflict 与 usage outcome。
4. **外部 telemetry rollout 仍需独立 canary**：fake collector 与受控 route 测试不能证明某个
   实际 Collector、Claude/Codex 版本或宿主网络的端到端遥测协议完全可用。
5. **完整 deterministic suite 仍有已分类基线红项**：`test_sim1_mock_reaches_unique_terminal_without_duplicate_attempts`
   可在未修改的基线独立复现，原因是 fanout worker worktree HEAD 与 immutable contract
   `base_commit` 不一致；它不属于本候选的 Evolution 环境准入回归，但在正式发布前应由对应
   owner 解决或明确接受。
6. **Developer Preview**：Web/API、事件 schema、Provider capability 和交互合同在 1.0 前
   仍可能发生不兼容变更。

## 已有验证证据

- Channel attention、流式合同隔离、Operator Inbox Decisions 和 Kanban AskUserQuestion 通过
  后端 focused suites、Web unit/typecheck/build，以及 Docker Playwright 的桌面与移动端验证；
  现有 Channel/Task 记录保留逐项命令和结果。
- Provider telemetry/OTLP 的 focused suite、fake collector、读取 API 与 Docker Playwright
  验证通过。相同最小 light workflow 的 A/B 保持 canonical task-map 与语义事件一致，候选仅
  增加脱敏 telemetry/exporter/attention 投影。
- Self-Evolution P0-P2 的 mock E2E 覆盖 evaluator registration、4 条 trial、comparison、
  proposal、approval、canary、retain/revoke、challenge、Pareto variant 与 export/import；
  影响闭包为 `189 passed`。
- 已归档的真实 Codex comparison 运行 baseline/candidate 各两次，裁决为
  `candidate_better`，但 apply mode 保持 `proposal_only`；无人干预低风险路径另行完成 3 次
  provider execution、独立 canary 与 `active_retained` 验证。
- 当前候选的环境准入闭环验证了真实 `codex-cli 0.148.0`、认证 probe、冻结 snapshot、
  isolated baseline、Run Archive 和清理；focused regression 为 `26 passed`，serial tier 为
  `10 passed`，flow smoke 为 `10 passed`，pre-merge sentinel 为 `51 passed`。

正式 tag 前应从最终版本提交重新执行：

```bash
uv sync --extra dev --extra web
uv run python scripts/dev-verify.py run --base <release-base>
npm --prefix web run build
uv run python scripts/manual-docs.py check
bash scripts/dev-premerge-gate.sh
bash scripts/run-flow-smoke.sh
```

随后执行隔离的真实 Provider、Docker Playwright、OTLP collector canary，以及启用外部集成时的
Feishu release E2E；这些 tier 不能由单元测试或绿色 Web 面板替代。

## 文档入口

- [中文 README](../../README.zh-CN.md)
- [English README](../../README.md)
- [版本记录](../../CHANGELOG.md)
- [Channel Conversation-to-PRD 设计](../design/166-channel-conversation-to-prd-and-polynoia-alignment-design.md)
- [Provider Native OTel 与 Operations 设计](../design/175-provider-native-otel-and-operations-observability-integration-design.md)
- [Self-Evolution 可信性与能力积累设计](../design/176-self-evolution-integrity-and-capability-accumulation-design.md)
- [Evolution 环境能力闭环设计](../design/177-evolution-environment-capability-closure.md)
- [Metrics、Observability 与 Operations](../manual/21-metrics-observability-operations.md)
- [自我进化与能力积累](../manual/23-self-evolution-learning.md)
- [Apache License 2.0](../../LICENSE)
- [免责声明](../../DISCLAIMER.md)

## 总结

v0.0.6 不是以更多 Agent 自治替代工程约束。它让多 Agent 对话中的可见回答、人工决策和
机器合同更清晰，让运行健康信号可观测但不篡改交付真相，并让经过验证的经验在明确的评估、
环境与回退边界内积累为可复用能力。
