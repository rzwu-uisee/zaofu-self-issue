# Adversarial Review：通过结构化分歧实现基于证据的 Agentic Code Review

Eric S. Qiu\*¹  
Joyce Gill\*²

\* 同等贡献。  
¹ Cornell University。  
² Stanford University。  
通信作者：Eric S. Qiu、Joyce Gill `<sq225@cornell.edu, joycegill@stanford.edu>`。

发表于 *Proceedings of the 43rd International Conference on Machine Learning*，Seoul, South Korea。PMLR 306，2026。版权归作者所有，2026。

> 翻译范围：原 PDF 第 1–8 页。第 9 页及之后内容未翻译。

## 摘要

早期的 multi-agent LLM 系统经常使用角色分离的团队，但随着 agent 数量增加，在 repository-level coding task 上获得的收益会越来越小。近期的另一类方案把 agent 当作被动工具，也就是 subagent，但这又完全去掉了 agent 互动所带来的好处。我们研究的是：subagent 范式能否支持一种折中方案——以最少量的 agent 合作，避开大型 multi-agent 团队的开销。

我们提出 Adversarial Review（AR），这是一套最简的协作式 code review 协议。在其中，main coding agent 会与一个 reviewer agent 和一个 critic agent 合作。Reviewer 负责评估代码；在 main agent 开始编辑之前，Critic 会通过结构化分歧来审计这份 review。在 LiveCodeBench 上，AR 在所有受测方法中取得最高通过率；它只使用三个 agent，却超过了一个五 agent baseline。在 SWE-PRBench 上，朴素 AR 暴露出一种 false consensus 失败模式：agent 在证据不充分时也会逐渐趋于一致。不过，只需进行一次 prompt 迭代，明确加入分歧要求，就能在所有受测方法中取得最高 F1。在 SWE-bench Verified 上，AR 相比 baseline 在 repository-level coding task 中也有提升。

综合来看，AR 表明，协作式 code review 并不需要大量 agent，也不需要复杂的通信结构；真正需要的是让分歧保持最小化、结构化，并且以证据为依据。

## 1 引言

基于 LLM 的 coding agent 越来越多地被部署来解决真实的软件工程任务，从处理 GitHub issue 到审核 pull request 都包括在内（Jimenez et al., 2024）。随着这些 agent 的能力增强，一个关键设计问题也随之出现：应该如何组织多个 agent，让它们能够可靠地协作完成 coding task？这个问题对自动化 code review 尤其重要，因为 agent 不仅要生成正确的修复，还必须提出真正值得关注的问题。

早期的 multi-agent LLM 系统会通过增加更多 agent 来提高质量。这些 agent 被放进角色分离的团队里，通过分配生成、review、debate 或汇总输出等角色来模仿人类的劳动分工（Islam et al., 2024；Huang et al., 2023；Pan et al., 2025；Islam et al., 2025）。不过，近期研究表明，在复杂的 repository-level coding task 上，增加同级 agent 的数量可能带来越来越小、甚至为负的收益。部分原因是，不受约束的通信会引入协调开销，并带来它自己的失败模式（Smit et al., 2023；Kim et al., 2025）。与此相反，许多生产级 coding agent 已经趋向“一个 main agent 加多个 subagent”的范式：subagent 通过 tool call 被调用，而不是作为独立协作者存在（Anthropic, 2024）。

我们要问的是，这两种范式之间是否存在一个有效的中间地带：一个“main agent 加 subagent”的系统，能不能让独立 agent 进行轻量合作，同时又不承担大型角色分离团队的开销？我们的目标，是设计一套最简协议：既保留 agent 互动的好处，又把通信限制在足够小的范围内，让它在真实 coding task 中仍然实用。

我们提出 Adversarial Review（AR），一套从朴素 zero-shot 代码生成逐步构建出来的协作式 coding 协议。每一步都会加入一个新的设计选择，而这个选择来自上一步中可以测量的失败。我们先在 LiveCodeBench（LCB）（Jain et al., 2024）上评估这套构造过程，然后从两个方向进一步检验最终协议：在 SWE-PRBench（Kumar, 2026）上考察 review 质量，在 SWE-bench Verified（Jimenez et al., 2024）上考察 repository-level repair。

我们使用两种执行模式。在 LCB 和 SWE-PRBench 上，由一个 Python orchestrator 强制执行严格受控的方法对比；在 SWE-bench Verified 上，我们把每种方法写成可移植的纯文本 `SKILL.md` 协议，再由 Claude Code 等 autonomous agent 按照协议执行。

### 图 1 中的流程文字

- Artifact version N（代码或计划）。
- 内循环：针对冻结 artifact 的 review 收敛过程。
- Reviewer 审核 artifact（同时查看 change log），生成 $Review_k$。
- Critic 评估并编辑 $Review_k$（一个全新、独立的 critic agent）。
- 判断 review 是否一致。
- 如果不一致，Reviewer 回应 $Review_{k+1}$，继续内循环。
- 如果一致，判断是否在第一次 review 时就收敛且发现零个缺陷。
- 如果是，则 PASS。
- 如果不是，则 main agent 编辑 artifact，生成 version $N+1$，同时更新 change log，然后重复流程。

**图 1：** Adversarial Review（AR）的工作流。Main agent 首先生成 artifact version $N$，也就是代码或计划。随后，协议进入一个 artifact 被冻结的内循环：reviewer $R$ 生成 $Review_k$，critic $C$ 对这份 review 进行评估，并且可能修改它；$R$ 再进行回应，直到获得一份一致的 review。如果 review 在第一轮就收敛，并且没有发现任何缺陷，artifact 会立即被接受。否则，main agent 编辑 artifact，生成 version $N+1$，更新 change log，然后重复整个过程。这里最关键的分离是：内循环只交换 review 文本，而 artifact 的编辑只发生在外循环中。

我们有三项贡献。第一，我们表明，在“main agent 加 subagent”的范式里，一个最简 reviewer–critic 循环可以改善 agentic coding 和 program repair。第二，我们识别出协作式 agent 系统的一种核心失败模式：agent 可能会追求达成一致，而不是追求正确。第三，我们表明，结构化分歧能够可靠地修复 false consensus，并在三个 benchmark 上同时提高代码生成质量和 review 质量。

## 2 相关工作

**代码的自我改进与外部验证。** Test-time refinement 方法通过加入批评、反馈或修改来改善模型的初始输出。Self-Refine 让同一个模型同时担任 generator、反馈提供者和 refiner，把修改限制在单一模型自己的判断循环中（Madaan et al., 2023）。不过，后续研究表明，在没有外部反馈时，language model 并不能可靠地自行纠正推理错误（Huang et al., 2024）。AR 建立在 test-time refinement 之上，但做了两处改变：它把 reviewer 和 critic 两个角色分开，并且让分歧成为协议的显式组成部分，而不是一项可选的自检。

**Multi-agent debate 与结构化分歧。** Multi-agent debate 让不同模型实例在 Judge 或最终答案被选出之前交换论点，通过 agent 之间的互动来改善推理、事实性或决策质量（Du et al., 2024；Liang et al., 2024）。不过，debate 式系统可能对 prompt 和协议选择十分敏感，也可能无法超过成本更低、没有 debate 的 baseline（Smit et al., 2023）。Multi-Agent Review Systems（MARS）则通过取消圆桌式 debate 来降低开销：多个独立 reviewer 分别给出意见，再由一个 meta-reviewer 进行汇总（Wang et al., 2025）。AR 处在两者之间。它保留互动，但把通信限制在一条通道里，并规定明确的分歧类型。目标不是开展开放式 debate，而是在 main agent 编辑或 commit 之前，提出以证据为依据的反对意见。

**用于代码生成与 review 的 multi-agent 系统。** 一些 coding system 使用角色分离的 agent，把编程工作拆成多个专业子任务。MapCoder 把 competitive programming task 分解为样例回忆、规划、代码生成和 debugging（Islam et al., 2024）。AgentCoder 把实现、测试设计和测试执行分开（Huang et al., 2023）。CodeSim 使用 simulation-driven planning 和 debugging 来引导代码生成（Islam et al., 2025）。CodeCoR 使用一个 self-reflective multi-agent framework，对生成的代码进行剪枝、完善和修复（Pan et al., 2025）。在 code review 方面，CodeAgent 研究了用于自动化 review 的 communicative agent，并加入一个 QA-Checker，让生成的 review 意见与原始问题保持一致（Zhang et al., 2024）。AR 有意比这些系统更小。它没有增加许多专业 coding 角色，而是询问：在“main agent 加 subagent”的工作流中，一个最简 reviewer–critic 组合能不能提供可靠、基于证据的 review。

**Agentic coding system 与可移植协议。** 生产 agent 系统越来越多地依赖一个 main agent，让它调用工具、subagent 或可复用的指令包（Anthropic, 2024）。在这种环境里，subagent 是针对具体子任务启动的独立 agent 实例，而 Skill 则是通过 `SKILL.md` 文件激活的可移植指令包（Anthropic, 2026；2025）。这促使我们用两种形式评估 AR：一种是用于严格受控对比的 Python orchestrator；另一种是纯文本 Skill，用来测试 autonomous coding agent 能否在没有外部强制执行的情况下，遵循相同的协作式 review 协议。

**Coding workflow 中的人类—agent 协作。** 越来越多的研究开始考察 coding agent 应该如何沟通进度、处理歧义并吸收用户反馈（Li et al., 2022；Kumar, 2026）。Code review 是人类—agent 协作的一种自然接口：review agent 不仅要找到 bug，还要生成开发者可以采取行动、接受或重新引导的发现。AR 通过明确展示 subagent 之间的分歧来推进这一方向，让最终传递给工作流监督者的 review 信号以具体代码证据为基础，而不是未经检查的猜测。

**Coding 与 review benchmark。** LiveCodeBench 使用持续更新、能够抵抗数据污染的 benchmark 来评估代码生成（Jain et al., 2024）。SWE-PRBench 根据 pull request 反馈评估 AI code review 的质量（Kumar, 2026）。SWE-bench 则通过编辑 repository 并运行测试，评估 agent 能否解决真实 GitHub issue（Jimenez et al., 2024）。这些 benchmark 把三个经常被混为一谈的主张区分开来：一种方法能否解决独立的编程问题；它能否帮助完成 repository-level repair；以及它能否生成有用的 review comment。我们利用这种区分，把 AR 同时作为 coding 协议和协作式监督协议来评估。

## 3 在 LiveCodeBench 上构建 AR

本节逐步构建 Adversarial Review 协议。每个小节都会加入一个设计选择。每项 LCB 结果报告两个指标：全部 105 道 stdin-style task 上的通过率（pass/105），以及其中 57 道 hard-tagged task 上的通过率（pass-on-hard/57）。完整结果见表 1。不同方法真正拉开差距的地方是 hard tier。在后续所有 benchmark 中，全部 agent 和 subagent 调用都使用 Claude Sonnet 4.5 Medium Reasoning。

### 3.1 Zero-shot → Self-Refine：加入验证

Zero-shot 直接写代码，没有任何验证步骤，所以我们加入一个验证步骤。Self-Refine（Madaan et al., 2023）让同一个 agent 批评自己的代码，然后再修改。在 LCB 上，改善几乎无法察觉。Zero-shot 的通过率为 77%，Self-Refine 的通过率也是 77%。原因是 critic 与 generator 是犯着同样错误的同一个模型，所以 critic 很难发现 generator 漏掉的问题（Olausson et al., 2024）。这促使我们使用一次单独的模型调用来充当 critic。

### 3.2 Self-Refine → Single-reviewer：外部验证

Single-reviewer 使用一次单独的模型调用。具体来说，main agent $M$ 编写代码，reviewer $R$ 审核 $M$ 的代码，然后 $M$ 修改一次。在 LCB 上，通过率为 77%，pass-on-hard 为 36/57。表现仍然与 Zero-shot 处在同一个集群里。一个可能的问题是，单个外部 reviewer 的方差很大：它可能什么都不检查就全部批准，也可能提出许多细枝末节，却没有瞄准真正的问题。这促使我们并行运行多个独立 reviewer。

### 3.3 Single-reviewer → Two-reviewers：独立性降低方差

两个独立样本应该能够降低方差。具体来说，$R_1$ 和 $R_2$ 独立审核 $M$ 的代码，然后 $M$ 根据两者意见的并集修改一次。在 LCB 上，通过率为 75%，pass-on-hard 为 34/57。这仍然处于 Single-reviewer、Self-Refine 和 Zero-shot 的噪声范围内。四种方法集中在同一个水平，朴素验证无法突破这一水平。这促使我们追求 agent 多样性，而不是增加更多独立样本。

### 3.4 Two-reviewers → MARS：有边界的多样性

Multi-agent debate（MAD）是 multi-agent LLM 系统中增加多样性的标准方法（Du et al., 2024）。不过，MAD 有两种成本，而且会随着 agent 数量和轮数增加。第一种是通信开销，因为每个 agent 都必须阅读其他所有 agent 的消息。第二种是通过率甚至可能随着系统扩大而下降（Smit et al., 2023；Kim et al., 2025）。

MARS（Wang et al., 2025）取消 reviewer 之间的消息传递，从而降低 MAD 式通信成本。具体来说，一个 author agent 先生成初始解答。三个 reviewer 并行工作，每个 reviewer 都输出一份结构化结果，其中包括二元决策（accept 或 reject）、自行报告的置信度、理由，以及在 reject 时明确指出的错误。随后，一个独立的 meta-reviewer subagent 会读取三份 review 和候选解答。Meta-reviewer 会生成一份统一输出，其中包括最终决策、理由、reject 时的修改建议，以及推荐答案。如果 meta-reviewer 拒绝，author 会根据建议修改解答，然后针对修改后的解答启动一轮全新的 review。当 meta-reviewer 接受解答，或者达到固定的 $K=2$ 轮上限时，循环结束。

在 LCB 上，MARS 是第一个通过率明显高于 Zero-shot、Self-Refine、Single-reviewer 和 Two-reviewers 的方法。相对于四种方法形成的集群，MARS 在 pass/105 上提高了 5 个百分点（原文此处写作 85%），在 hard task 上比 Single-reviewer 多通过 3 道（原文此处写作 43/57）。不过，MARS 有两个问题。第一，相比每道任务使用五个 agent 所带来的 token 成本——一个 author、三个 reviewer 和一个 meta-reviewer——提升幅度很小。第二，MARS 虽然避免了 MAD 的通信开销，却也完全去掉了所有 agent 互动。于是，一个很自然的问题是：我们能不能通过 agent 互动改善 coding performance，同时又不承担 MAD 的开销，而且使用更少的 agent？

### 3.5 MARS → AR：两个 agent 的互动

一种仍然允许有意义合作的紧凑互动结构，可以由一个 reviewer 加上一个负责批评该 reviewer 的 critic 组成。我们把它称为 Adversarial Review（AR）。如图 1 所示，这套协议围绕 main agent $M$ 连续生成的多个 artifact version 运行，其中 artifact 可以是代码，也可以是计划。在第 $N$ 次外循环中，$M$ 生成 artifact version $N$，同时提供一份 change log，记录 artifact 是如何演变的。

然后，AR 进入一个永远不会编辑 artifact 的内循环。一个独立 reviewer subagent $R$ 会检查 artifact，并生成 $Review_k$。接着，一个全新的 critic subagent $C$ 会评估这份 review，并且可能修改或质疑它。如果 review 和 critique 尚未达到一致，协议就回到 $R$，由它给出修改后的 $Review_{k+1}$。Reviewer 与 critic 的这一轮交换会一直重复，直到 review 收敛；为了控制成本，也可以在达到 5 轮内循环上限时停止。因此，内循环是在固定 artifact 上执行 review 的流程，而不是编辑 artifact 的流程。这意味着，我们可以把内循环单独作为一种 code review 方法来评估，详见第 4 节。

得到一致的 review 后，协议会退出内循环，并执行外循环决策规则。如果 $R$ 和 $C$ 立即收敛，而且没有发现任何缺陷，AR 就会终止并接受 artifact。这就是 first-pass termination rule。否则，一致的 review 会返回给 main agent $M$；$M$ 编辑 artifact，生成 version $N+1$，更新 change log，然后针对更新后的 artifact 发起一轮新的内部 review。这样，AR 会在两种循环之间交替：（i）内循环：针对 artifact 的 review 文本寻求一致；（ii）外循环：只有在产生稳定的 review 信号之后，main agent 才会编辑 artifact。

在 LCB 上，AR 在我们测试的六种方法中通过率最高：通过率为 87%，pass-on-hard 为 43/57。具体来说，AR 比 MARS 高出 5 个百分点，而 review 角色更少：AR 有 2 个 review 角色，也就是 $R$ 和 $C$；MARS 有 4 个，也就是三个 reviewer 加一个 meta-reviewer。

**表 1：LCB 结果。** 最佳结果以粗体表示。前四种方法的通过率集中在同一水平，MARS 突破了这个集群；在实现突破的方法中，AR 使用最少的 agent，得分却最高。

| 方法 | pass / 105 | pass-on-hard / 57 | agent 数量 |
|---|---:|---:|---:|
| Zero-shot | 77% | 35/57（61%） | 1 |
| Self-Refine | 77% | 35/57（61%） | 1 |
| Single-reviewer | 77% | 36/57（63%） | 2 |
| Two-reviewers | 75% | 34/57（60%） | 3 |
| MARS | 82% | 39/57（68%） | 5 |
| **AR** | **87%** | **43/57（75%）** | **3** |

表 1 汇总了全部六种方法。LCB 上的评估首先验证了我们要寻找的中间方案。AR 表明，在“main agent 加 subagent”的范式中，两个相互互动的 review agent，可以比四个互不互动的 review agent 表现得更好。

### 3.6 紧凑的互动结构

在这些方法中，AR 是仍然保留互动、角色数量最少的方法。AR 有三个角色：$M$、$R$ 和 $C$，其中 $R$ 与 $C$ 彼此互动。这与 Claude Code、Cursor 和 Copilot 等生产 agent system 使用的“main agent 加 subagent”结构一致（Anthropic, 2024）。如果去掉 $C$，循环就变成 Single-reviewer；如果加入更多 reviewer 或更多轮次，循环就变成 MARS 式或 MAD 式。

AR 在 LCB 上获得最高通过率，但我们还会从两个方向继续检验这套协议。第 4 节考察 SWE-PRBench 上内循环的 review 质量，第 5 节考察 SWE-bench Verified 上的真实 agentic 场景。

## 4 SWE-PRBench 上的 Review 质量

本节做两件事。第一，它表明 AR 在 LCB 上取得的领先成绩，不能直接迁移到 review 质量 benchmark。第二，它找出失败模式，并用一次 prompt 迭代修复这些问题。这个修复指出了一条适用于任何协作式 coding agent 的设计原则：当多个 agent 通过互动来共同做出 review 判断时，协议应当强制要求分歧以显式、基于证据的形式出现，否则，最后得到的 review 可能还不如一个独立 reviewer 可靠。

### 4.1 实验设置

SWE-PRBench（Kumar, 2026）包含 100 个真实 GitHub PR diff。Agent 的任务是审核 diff。一个 GPT-5.2 Judge 会把 agent 的 comment 与真实人工 reviewer 的 comment 进行匹配。该 Judge 与人工标注者之间的 Cohen's kappa 为 0.75；这是一项一致性分数，其中 1.0 表示完全一致，0 表示与随机一致相当。评估指标是匹配 comment 的 F1。

我们把对比限制在四种方法：AR、MARS、Two-reviewers 和 Single-reviewer。我们排除了 Zero-shot 和 Self-Refine，因为它们不适用于只做 review 的 benchmark。评估这四种方法，能够回答 AR 的互动式 $R$–$C$ 结构是否会比并行运行 reviewer 的方法生成更好的 review。

### 4.2 朴素 AR 表现更差

朴素 AR 协议在 SWE-PRBench 上的 F1 为 0.457，在这个子集的四种方法中最低。另外三种方法的 F1 都在 0.50 左右。因此，在 review 质量这一场景中，AR 的互动式 $R$–$C$ 循环反而比不使用这种循环的方法获得更低的 F1。第 4 节接下来的部分会找出原因并修复它。表 2 给出排行榜。第 4.3 节的两个案例揭示两种失败模式，第 4.4 节说明解决两者的 prompt 迭代。迭代后的协议称为 AR with text constraint，F1 达到 0.533，在这个子集中最高。

**表 2：SWE-PRBench 中与 review 相关的子集。** 最佳结果以粗体表示。“AR”和“AR with text constraint”采用相同的 $R+C$ 结构，只有 prompt 不同。Agent 数量以及它们之间的连接方式均未改变。Prompt 迭代见第 4.4 节。

| 方法 | F1 | N |
|---|---:|---:|
| **AR with text constraint** | **0.533** | **100** |
| Two-reviewers | 0.503 | 100 |
| MARS | 0.501 | 100 |
| Single-reviewer | 0.495 | 100 |
| AR | 0.457 | 100 |

### 4.3 两种失败模式（案例研究）

**案例 A——过度拆分。** 在 AR 中，reviewer $R$ 写出一份 review，其中包含一个真实 bug 和两到三个带有保留语气的 concern。Critic $C$ 同意 $R$ 标出的所有问题，并且在此基础上又增加一个推测性 bug。随后，一个格式 review 步骤把每个问题都转换成单独的 comment。Judge 判定 5 条 comment 中有 3 条属于凭空捏造，因为它们过于单薄或过于推测。其机制是：$R$–$C$ 循环不断增加发现，却没有对它们进行筛选。$R$ 经常使用保留性表达，$C$ 又确认了 $R$ 的大部分保留意见。结果就产生了太多单薄 comment，被 Judge 判为凭空捏造。在这道任务上，AR 的 F1 为 0.250。

**案例 B——$C$ 向 $R$ 让步。** 在 AR 中，reviewer $R$ 生成了一份 APPROVE review。Critic $C$ 提出一个真实问题，我们已经通过人工方式验证了它。$R$ 用很弱的信号反驳 $C$：$R$ 使用文件级论点，却没有引用任何代码。$C$ 最终让步，结论翻转为 AGREE，于是真实 bug 从最终 review 中消失了。案例 B 揭示了一种结构性机制：协议推动 $R$ 与 $C$ 达成一致，所以 $C$ 往往会选择同意。即使 $R$ 是错的，它也可以通过写一段听起来很自信的反驳来结束分歧。看起来像“结构化分歧”的过程，变成了 false agreement。在这道任务上，AR 的 F1 为 0.286，而 MARS 抓住了同一个 bug，F1 为 0.667。

### 4.4 AR with text constraint

我们在文本中明确引导 pushback constraint，形成 AR with text constraint。其他所有内容保持不变。Text constraint 规定，所有被标记的问题都必须以具体证据为依据。具体来说，$C$ 原先要在两个 verdict（AGREE 或 DISAGREE）之间选择，现在则必须从下面三个选项中选择一个：

- `AGREE`：$C$ 接受 $R$ 的 review。
- `DISAGREE EVIDENCE: <code citation>`：$C$ 引用与某个问题相矛盾的具体代码。
- `DISAGREE CONCERN: <epistemic objection>`：$C$ 提出一个无法指向矛盾代码的认识论异议。

$R$ 的回应规则取决于它看到的分歧类型。看到 `AGREE` 时，$R$ 原样保留所有问题。看到 `DISAGREE EVIDENCE` 时，$R$ 根据被引用的代码修改该问题。看到 `DISAGREE CONCERN` 时，$R$ 必须引用 diff 中能够确认 bug 的代码并保留该问题，或者引用能够否定 bug 的代码并删除该问题。

### 4.5 结果

AR with text constraint 的 F1 为 0.533，在四种方法的子集中最高。为了保持最简协议结构，我们到这里就停止迭代：只使用一个 reviewer $R$ 和一个 critic $C$，只修改 prompt。

案例研究表明，当两个 LLM agent 被要求对一个联合输出达成一致时，它们往往会相互同意，却不一定能找到真相。所以，协议必须包含明确的 pushback。没有 pushback 时，看似“结构化分歧”的过程会变成 false agreement。

## 5 SWE-bench Verified 上的真实 Coding

### 5.1 实验设置

SWE-bench Verified（Chowdhury et al., 2024）包含来自开源 Python repository 的 500 个真实 GitHub issue。Agent 必须阅读 issue、浏览 repository、找到 bug 并输出 patch。指标是 pass@1，通过官方 Docker harness 测量。

这里的执行模式与第 3 节和第 4 节不同。我们使用拥有完整工具权限的 Claude Code。Claude Code 会读取 `adversarial-review` Skill；它本质上只是一份 Markdown 文档，也就是 `SKILL.md`，用纯文本记录 AR 协议，而不是使用 Python orchestration logic。

以 Skill 形式评估 AR 有一些理想特征。首先，按照设计，Skill 形式允许 main LLM agent 自己驱动协议。具体来说，agent 可以决定何时把 $R$ 和 $C$ 作为 Task subagent 调用，也可以自己决定何时编辑。我们在 LCB 和 SWE-PRBench 上评估 AR 时，特意使用 Python orchestration logic 来进行严格的执行对比，这更偏向结构固定的 agentic workflow。另一方面，更一般意义上的 LLM agent 可以自己决定何时调用、以及如何安排自身工作流。Coding agent 通常需要灵活性和创新性的决策能力来解决复杂 coding task，因此属于后者。所以，Skill 形式的 AR 为在灵活 coding agent 上评估协议提供了一种自然方式。

我们只在 SWE-bench Verified 上评估三种方法，因为预计每种方法跑完整个 benchmark 都需要 300 多个小时。我们选择 Zero-shot 作为朴素 Claude Code baseline，选择 MARS 作为测试过的最强 multi-agent baseline，并选择 AR 作为本文提出的方法。这里使用的 MARS 主体与第 3.4 节和第 4 节相同，只是表达成类似的 Skill 风格文档，让 agent 可以自主遵循；详见第 A.3.3 小节。

### 5.2 结果

如表 3 所示，AR 在 $N=500$ 上的通过率达到 75.2%。同一个 Claude Code scaffold 上的 Zero-shot 为 71.6%，MARS 为 72.6%。不过值得注意的是，AR 使用的 token 大约是 Zero-shot 的 4.5 倍（见第 6 节），也就是说，它用 token efficiency 换取了性能提升。

**表 3：SWE-bench Verified 上三种方法的通过率：Zero-shot、MARS 与 AR。**

| 方法 | 通过率（%） | N |
|---|---:|---:|
| AR | 75.2% | 500 |
| Zero-shot | 71.6% | 500 |
| MARS | 72.6% | 500 |

### 5.3 案例研究

我们使用两个案例进一步分析 AR 在 SWE-bench Verified 上的结果。第一个案例展示 AR 何时有帮助：reviewer–critic 循环推动 agent 找到 root cause。第二个案例展示 AR 何时会造成伤害：同一个循环放大了一项推测性 concern，让 patch 超出了 issue 范围。这两个案例都来自 AR 的 Skill 形式评估。

**案例 1——结构化分歧有助于找到 root cause。** 任务：`matplotlib matplotlib-20826`。

问题发生在 `Axis.clear()` 重置每个 tick 的 keyword argument dictionary 之后。结果是，当用户对共享 axis 或 subplot axis 调用 `ax.clear()` 时，由 `label_outer()` 隐藏 label 等 tick visibility 设置会丢失。

Zero-shot 和 MARS 都修改了 caller：`lib/matplotlib/axes/_base.py`。它们的 patch 会在 reset 已经销毁设置之后，重新应用 tick visibility 设置。这修复了症状，却没有修复根本原因。Zero-shot 生成一份 45 行 patch，MARS 生成一份 50 行 patch；两者都没有通过 hidden test。

相比之下，AR 修改的是 callee：`lib/matplotlib/axis.py`。AR 没有在 reset 之后重新应用 visibility 设置，而是删除了范围过大的 reset，直接保留相关的 tick keyword 状态：

```diff
- self._reset_major_tick_kw()
- self._reset_minor_tick_kw()
+ self._major_tick_kw['gridOn'] = ...
+ self._minor_tick_kw['gridOn'] = ...
```

这一差异非常重要。Zero-shot 和 MARS 在 public API 层修复可见症状。虽然 AR 的 reviewer subagent $R$ 在第一轮 review 中也犯了同样的错误，但 critic subagent $C$ 立刻发现了这个错误，agent 也马上达成一致。最终，AR 真正修复了实现层中销毁状态的操作。AR 的 patch 更短，大约 20 行，也是唯一通过 hidden test 的 patch。我们把它理解为这样一个案例：reviewer–critic 循环帮助 main agent 沿着调用链多深入一层，从修复症状转向修复 root cause。

**案例 2——结构化分歧放大 scope creep。** 任务：`astropy astropy-14182`。Issue 要求 agent 为 RST writer 加入一个 `header_rows` 参数。

Zero-shot 用一份最简的 24 行 patch 解决了任务，把参数贯穿 `__init__` 和 `write`。Patch 没有超出 issue 范围，并且通过了 hidden test。

AR 生成了一份更大的 32 行 patch。它加入了 `header_rows`，但还删除了 `SimpleRSTData` 中一个稳定的 class variable `start_line=3`，并加入新的 `read()` 方法。这些额外修改并不是 issue 所要求的。Trace 清楚显示了过度编辑的来源：在规划过程中，reviewer 问道：“如果用户单独调用 `read()` 会怎样？”Critic 没有以超出范围为由拒绝这个问题。随后，main agent 实现了这项推测性修改，patch 也因此没能通过 hidden test。

这个案例展示了 AR 在 SWE-bench Verified 上的主要失败模式。Reviewer–critic 循环能够改善 root-cause analysis，但也可能把听起来合理的 concern 变成不必要的修改。换句话说，当分歧把 agent 引向真实 bug 时，AR 会带来帮助；当分歧让修复超出 issue 范围时，AR 会造成伤害。这说明，未来版本的 AR 可以像 AR with text constraint 中对“以证据为依据的分歧”所做的规定一样，把 scope discipline 也设为显式要求（第 4.4 节）。

## 6 各 Benchmark 上的成本—质量位置

虽然 AR 会用 token 成本换取性能，就像第 5.2 节所展示的那样，但它仍然位于我们评估的三个 benchmark 的成本—质量 Pareto frontier 上。Pareto frontier 是这样一组方法：不存在另一个方法既比它便宜，又比它更好。图 2 绘制了每种方法在每道任务上的 token 中位数与其 benchmark 表现之间的关系。

结论是，在三个 benchmark 上，AR 使用的 token 都比 Zero-shot 更多。额外 token 让 AR 在每个面板上都取得 Pareto-frontier 性能。所以，在我们测试的方法中，没有任何一种方法同时比 AR 更便宜、表现也更好。

### 图 2 中的文字与图注

- 横轴：每道任务的 token 中位数（对数尺度）。
- 面板（a）LiveCodeBench，纵轴：pass-rate（%）/ 105。方法包括 ZS、Self-Refine、Single-R、Two-R、MARS 和 AR。
- 面板（b）SWE-PRBench，纵轴：F1。方法包括 Single-R、Two-R、MARS 和 AR with text constraint。
- 面板（c）SWE-bench Verified，纵轴：pass-rate（%）/ 500。方法包括 ZS、MARS 和 AR。

**图 2：** 每个 benchmark 上，每道任务的 token 中位数与质量之间的关系。三个面板的纵轴衡量不同内容：binary pass-rate 与 LLM-Judge F1。因此，图中主张的是每个面板内部的 trade-off，而不是跨面板比较。LCB 的 token 中位数根据每种方法的调用次数估算；SWE-PRBench 和 SWE-bench Verified 的 token 中位数则根据逐任务日志计算。

## 7 讨论与局限

**AR 之所以有效，是因为它把合作范围收得很窄。** 本文的主要结论不是增加更多 agent 能够改善 coding。事实上，我们一步步的构造过程表明了相反的情况：仅仅增加独立 reviewer 并不能稳定改善 LCB 表现；而 MARS 需要五个 agent——一个 author、三个 reviewer 和一个 meta-reviewer——才能获得比 AR 更小的提升。AR 真正有效的地方更具体：它把合作限制在一个很窄的接口上——reviewer 写 review，critic 审计这份 review。在这次交换中，artifact 保持冻结；只有 review 稳定之后才允许编辑。

这种分离非常重要，因为它能防止 agent 在还没有就“到底错在哪里”达成一致时，就一起重写解答。从这个意义上说，AR 虽然只是插入 main-agent workflow 的一个小型监督机制，但从行为上看，它最终形成了一个角色分工清晰的协作式 agent 团队。

**结果支持工具与团队之间存在中间地带。** 生产 coding agent 经常采用“main agent 加工具”的模式，其中 subagent 作为工具被调用。早期的 multi-agent coding system 则经常采用团队模式，让大量角色分离的 agent 生成并汇总输出。AR 处在两种设计之间。Main agent 仍然负责编辑 artifact，但 reviewer 和 critic 并不只是被动工具；在 main agent 采取行动之前，它们会彼此互动。

这种设计让 AR 获得了角色分离式合作的一些好处，却不需要 MAD 那样的大型圆桌 debate（Du et al., 2024；Smit et al., 2023；Wang et al., 2025）。SWE-bench Verified 的结果因此十分重要：即使 AR 只被写成一份 `SKILL.md` 协议，没有 Python orchestrator 强制执行每个步骤，它仍然能够改善性能。这说明，当角色和决策规则简单到足以用文字表达时，轻量协作协议可以在不同 agentic system 之间移植。

**False consensus 是可靠性失败，不只是性能失败。** SWE-PRBench 的结果说明，为什么协作式 AI system 不但需要跑 benchmark，还需要接受审计。AR 在 LCB 上表现最好，在 SWE-PRBench 上却是以 review 为中心的方法中表现最差的一个。案例研究让失败变得清晰可见。在一个案例中，reviewer 提出太多依据薄弱的发现，critic 又确认了这些发现；在另一个案例中，critic 提出一个真实问题，之后却向 reviewer 一段自信的反驳让步。

两种失败拥有相同结构：agent 达成了一致，但这种一致没有足够证据支持。我们把这种失败模式称为 false consensus。False consensus 特别值得担忧，因为它看起来很像独立验证：两个 agent 表面上相互同意，但这种一致可能只是因为对话存在趋同压力。该失败模式揭示了，在一个 benchmark，也就是代码生成上表现优秀，并不能保证同样的方法在相关任务，也就是 code review 上也表现优秀。

此外，这种失败模式是结构性的——它来自 agent 的互动方式，而不是单个 agent 的质量。AR 通过显式展示分歧来处理这一问题，迫使双方在任何问题被定论之前，都先提出以证据为依据的反对意见。

**以证据为依据的分歧是一项关键设计。** AR with text constraint 通过加入文本约束来改善 SWE-PRBench 表现，而没有增加更多 agent。Critic 必须区分同意、以证据为依据的分歧，以及以 concern 为依据的分歧。Reviewer 随后必须使用代码证据回应，而不能只写一段听起来合理的反驳；这样一来，分歧就变成了一种可以审计的对象。

这是 AR 对 trustworthy AI 最重要的启示：评价 multi-agent oversight 时，不应该只看 agent 最后有没有收敛，还应该看通往收敛的路径有没有保留异议、证据和问责。我们的文本约束强制 agent 把分歧离散化为恰好三种类型之一，让 agent 分歧变得清晰、明确且透明。不过，textual constraint 可能既不是在 LLM agent 中强制 semantic constraint 的最佳方案，也不是最终方案。

虽然 LLM 的 instruction-following 能力会随着整体能力增长而持续增强，但当前的 LLM 仍然很容易忘记或忽略指令，尤其是在 context window 接近填满或已经超出之后。进一步研究引导 LLM 的方法，是一个与本文正交的研究方向，但它对构建 trustworthy AI 仍然十分重要。

**成本 trade-off。** AR 并不是没有成本的改进。在三个 benchmark 上，它使用的 token 都比 Zero-shot 更多。成本—质量图表明，在我们测试的方法中，AR 位于 Pareto frontier 上，但这并不意味着永远都应该使用 AR。对于简单任务，review 循环可能只是浪费计算。对于 issue 范围很窄的任务，额外 review 可能增加过度编辑的概率。当 repository 很复杂，或者 root-cause localization 比减少 token 成本更重要时，AR 才最有使用价值。

这指出了一个自然的未来方向：构建自适应版本的 AR，只在 main agent 不确定，或者 patch 涉及高风险代码时，才调用 reviewer–critic 循环。

**我们关于 agent 合作的主张是经验性的，不是形式化结论。** 我们并没有声称 reviewer–critic 循环普遍优于独立 reviewer、debate 或单 agent refinement。我们的结论来自三项经验性检验：在 LCB 上逐步构造协议；在 SWE-PRBench 上评估内循环 review 质量；以及在 SWE-bench Verified 上使用 Skill 形式完成 repository repair。这些场景能够相互补充，但没有覆盖所有 coding task 或全部 agent scaffold。

特别是，Python orchestrator 驱动的实验会在受控执行条件下隔离协议本身，而 SWE-bench Verified 实验测试的是限制更松的 Skill 形式指令。两种模式回答的是不同问题：第一种模式询问，该机制能否在严格工作流下发挥作用；第二种模式询问，把该机制交给 autonomous agent 后，它是否仍能保留下来。

**评估受到 benchmark 与 Judge 设计的限制。** LCB 衡量 competitive-programming 风格的正确性，SWE-PRBench 衡量与人类 pull-request 反馈的相似性，SWE-bench Verified 则通过 hidden test 衡量 repository-level repair。每个 benchmark 都只能捕捉质量的一个侧面。特别是，SWE-PRBench 依赖一个 LLM Judge，把生成的 comment 与人工 review comment 进行匹配。这让它适合比较 review 方法，但它可能会惩罚与人工 review 不同、却依然有效的 comment；也可能奖励表面形式与人工 comment 相似、却没有真正改善 patch 的 comment。

同样，SWE-bench Verified 衡量最终 patch 能否通过测试，却不能完整衡量 maintainability、minimality 或长期代码质量。案例研究通过考察机制，部分填补了这一空白；但要验证 AR 是否适合作为已经部署的 review assistant，还需要规模更大、覆盖面更全面的评估。

**未来工作应研究自适应且可审计的合作。** 协作式 coding agent 的下一步，是更好地控制何时调用 reviewer–critic 循环，以及这个循环如何与 main agent 的规划互动。具体到 AR，各种变体可以加入 scope-checking critic、在简单任务上跳过 review 的 confidence gate，或者使用 disagreement trace 产生训练信号，改善 agent 在代码任务上的 alignment。更广泛地说，我们的结果表明，review 协议应该围绕明确的失败模式设计，而不是围绕 agent 数量或互动轮数设计。

## 8 结论

我们在 LCB 上一步步构建了 AR。演进顺序是 Zero-shot、Self-Refine、Single-reviewer、Two-reviewers、MARS、AR。在我们测试的所有三项 benchmark 上，AR 都取得了受测方法中的最高分，同时仍然是“main agent 加 subagent”范式中一种紧凑的互动结构。我们的关键发现是：与其朴素地增加 agent 数量，不如设计结构化、显式且以证据为依据的分歧；后者可能更有效地提高代码生成质量和 code review 质量。
