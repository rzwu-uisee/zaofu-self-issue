# AutoResearch Agent 是如何失败的：对 100 个真实前沿研究任务进行端到端诊断评估

Yanlin Fei\*¹，Nazhou Liu\*¹，Xinmiao Yu\*¹，Shaolong Chen¹，Lei Li¹，Rahul Thapa²，Madalina Ciobanu¹，Qingqing Mao‡¹˒³，Ritankar Das¹˒³

¹ Prentis AI  
² Stanford University  
³ Titan Holdings

\* 同等贡献，按姓氏字母顺序排列。‡ 通信作者。

- 代码：https://github.com/PrentisAI/AutoResearchEval
- 数据：https://huggingface.co/datasets/PrentisAI/AutoResearchEval
- 网站：https://prentisai.github.io/AutoResearchEval-site/
- 邮箱：qmao@prentis.ai

> 翻译范围：原 PDF 第 1–19 页，止于第 19 页正文结论末尾；第 19 页的 References 及其后内容未翻译。

## 摘要

人工智能辅助科学研究已经有很长时间，但 large language model 和 agentic scaffold 的快速进步正在重塑整个领域：如今，一个系统可以从最初的 hypothesis 出发，完成一整个阶段的研究，一直推进到最终发表的论文。这种范式现在被称为 AutoResearch。然而，现有评估几乎无法说明这些 agent 是如何工作的，又会在哪里崩溃。任务范围很窄；评估衡量的是表现，而不是过程；失败诊断既缺乏系统覆盖，也缺少 artifact-level 可见性。

为了填补这一空白，我们提出 AutoResearchEval。它包含 100 个任务，均以七个科学领域中已经发表的前沿科学研究为基础，并覆盖完整研究生命周期：构思、检索、执行、分析、写作和 review。我们评估了八种 harness–model 组合，得到 800 条 autoresearch agent trajectory，并对过程进行标注。我们把这些发现整理成 ARFT（AutoResearch Failure Taxonomy），这是一套包含 45 种、以经验为依据的失败模式的框架。

为了实现可扩展、细粒度的归因，我们使用一条经过人类校准的 agent-as-a-judge 流水线，检查完整 trajectory 和中间 artifact。虽然失败模式遍布研究生命周期的全部阶段，但它们最终汇聚到同一个总体局限：当前 agent 缺少 metacognitive loop，也就是缺少这样一种能力——把自己生成的东西与找到的证据相互核对；发现站不住脚时进行修改；并质疑自己选择的路径是否合理。

相同模式在全部八种 harness–model 组合中反复出现，包括我们测试的最强模型。这说明缺陷位于 model 层，而不是任何一个特定 scaffold；orchestration 层面的干预能否弥补这一缺陷，仍然是一个开放问题，本文没有对此进行测试。我们公开发布 AutoResearchEval 和 ARFT，希望促进 autonomous scientific discovery 的持续研究与开发。

## 1 引言

人工智能辅助科学研究已经有几十年历史。直到最近，“AI for science”主要还意味着：用一个专业模型解决一个定义明确的任务，例如预测 biomolecular structure [11, 16, 19, 23, 28, 35, 42, 43]、加速材料发现 [1, 8, 26, 44, 46]，或者预测天气 [2, 20]；人类研究者仍然负责提出问题、构建 AI 工具本身、解释结果并传播研究发现。AlphaFold [16] 是这一范式的代表：它为一个存在已久的单一问题提供了里程碑式解法，但也仅限于该问题。

Large language model 与 agentic scaffold 的迅速进展正在重塑这一图景。把 foundation model、外部工具和 agentic workflow 结合起来后，一个系统可以从最初的 hypothesis 出发，完成文献 review、实验和分析，最后形成书面草稿。这种范式如今被称为 AutoResearch [18, 37, 49]。AI 正在逐步从单纯的科学工具，变成科学发现的参与者。被自动化的不再是一个孤立步骤，而是整个研究过程。于是，一个很自然的问题是：agent 在完整 AutoResearch 过程中，究竟能做得多好？

评估 autonomous research agent，需要刻画它在整个科学过程中的行为。近期工作已经朝这一目标迈出显著步伐：既有评估孤立研究技能的 benchmark [14, 29, 32–34, 36]，也有越来越有野心的端到端流水线 [4, 41, 45]。尽管已经有所进展，现有评估仍留下三个空白；这三个空白共同遮蔽了 agent 的工作方式及其崩溃位置。

**任务范围狭窄。** 现有 discovery benchmark 可以分成两类，但任何一类都没有在真实科学领域中覆盖完整研究生命周期。第一类把 agent 放进模拟或虚构世界，因为真实实验代价高昂；代价则是丧失真实感 [15, 22, 25, 40]。第二类以真实科学为基础，却把自己限制在可验证场景中，最常见的是 machine learning 或 software engineering task，因为它们的结果可以拿一个定义明确的目标来衡量 [6, 7, 27, 34, 36, 41, 45]。因此，所覆盖的科学领域同样很窄，真正开放式的 discovery task 被排除在外。

**评估衡量表现，而不是过程。** 几乎所有现有 benchmark 都把评分锚定在终点：是否与参考答案匹配、是否复现某个结果、或者是否达到已经发表的 SOTA。这会诱发 reward hacking：circular validation、grader-fitting 或数据泄漏都能让数字变好，却没有真正完成科学研究；分数也无法区分一条合理的 trajectory 和一条被“刷”出来的 trajectory [6, 14, 41]。它还把 long-horizon trajectory 压缩成单个标量，只报告某次运行失败了，却不说明为什么失败、在哪里失败、又是怎样失败的。因此，endpoint evaluation 可以给系统排名，却不能诊断系统。

**失败诊断缺少系统覆盖和 artifact-level 可见性。** 专家案例研究会深入检查单条 trajectory [10, 13, 17, 21, 31, 38, 49]，但无法推广到多个系统或多类任务。面向 scientific discovery 的分类工作 [3, 10, 24] 提供了更宽泛的类别，却来自规模很小的 corpus。最系统的一条路线，是针对 multi-agent 或 QA 场景开展 trace-level analysis [5, 9, 48]；不过，它标注的是对话 trace，因此 artifact-level failure 仍然不可见，而且其中的阶段是互动阶段，不是科学方法的步骤。

这些空白共同造成了一个关键盲点：当前评估可能告诉我们 agent 成功了，却很少揭示它如何运作，或者究竟在哪里崩溃。

为填补这一空白，我们研究 autonomous research agent 为什么失败，并产出两项 artifact。第一项是 AutoResearchEval：它包含 800 条 research trajectory，并带有 artifact-aware、process-level 失败标注。这些 trajectory 来自八种 harness–model 组合；它们在一套包含 100 个任务的 suite 上运行，覆盖七个前沿领域和完整生命周期的科学工作流——构思、检索与综合、执行、分析、写作和 review；任务则根据著名 venue 发表的论文构建。

任务包括两种体制：一种具有明确的 execution-feedback 信号，也就是人类 SOTA 或定量指标；另一种完全开放，没有这种信号。对于后者，我们有意评判 agent 过程的严谨性和 self-consistency，而不是它与 ground truth 的一致程度。标注由一个经过人类校准、能够感知 artifact 的 agent-as-a-judge annotator [51] 完成。它会完整阅读每条 trajectory，包括运行日志、生成的数据、代码和报告，而不是只看最终答案。我们把它与人工标注进行校准，并在事后抽查其输出。

为了系统标注并全面分析 AutoResearchEval，我们开发了第二项 artifact：ARFT。这是第一套面向 autonomous research agent 的系统且全面的失败 taxonomy。ARFT 以带标注的 trajectory 为经验基础，沿两个交叉轴组织 45 种失败模式：一条轴表示失败出现在哪个生命周期阶段；另一条轴表示它背后的 root cause。这样，表面上不同但机制相同的失败会被归为一组，而同一阶段的失败则可以分散到不同原因下（图 3）。AutoResearchEval 提供 research agent 在实践中如何失败的经验证据，ARFT 则提供一种结构化词汇，用来诊断、归因并最终缓解这些失败。

我们对 AutoResearchEval 的分析得到一个核心发现，它修正了现有的主流假设。虽然失败模式遍布研究生命周期的所有阶段，但最终都汇聚到同一个总体局限：当前 agent 缺少 metacognitive loop——也就是把自己的产出与找到的证据进行核对、在产出站不住脚时修改它，以及质疑自己所走路径是否合理的能力。

**贡献。** 总体来说，我们的核心贡献如下：

- AutoResearchEval：一套开源 collection，包含 800 条带有 artifact-aware、process-level 失败标注的 trajectory；同时发布底层任务 suite，其中包含 100 个任务、七个领域和完整生命周期，并以已发表的前沿科学为基础。它还包含一个完全开放式子集，该子集按过程而不是结果评分。
- ARFT：第一套面向 autonomous research agent 的系统性失败 taxonomy，组织了 45 种有经验依据的失败模式。
- 一个经过人类校准、能够感知 artifact 的 agent-as-a-judge，用于 ARFT 分类。它会完整阅读每条 trajectory，而不是只看最终答案，因此能够支持对失败模式的分析与理解。
- 对这些 agent 在何时、为何失败所做的系统诊断：我们把三个认知层 root cause 全部追溯到同一个 metacognitive deficit，也就是缺少闭合的 metacognitive loop。该诊断把缺口定位在 model 层——相同模式在所有八种 harness–model 组合中反复出现——并指出，下一代 language model 如果要实现真正的 scientific discovery，就必须具备 metacognitive loop。Orchestration 本身能否弥补这一缺口，是本文没有测试的开放问题。

### 图 1 中的结构与文字

**a. 任务构建**

- 输入 5,878 篇论文。
- Query 仅含 `Premise`、`Tension`；Target 为保留不公开的 `KeyClaims`、`Conclusion`。
- 从 5,878 篇论文中筛选得到 100 个任务；排除 wet-lab experiment、survey paper 和 gated data。
- 任务构成：70 个 open-ended，30 个 target-anchored。

**b. AutoResearch trajectory**

- 共运行 800 个 harness × model 组合实例，即 8 种 scaffold × model 组合。
- 单次运行包含六个阶段，并允许 revision：A 构思、B 检索、C 执行、D 分析、E 写作、F 自我验证。
- 案例中的预期路径依次为：探索开放式想法 → 检索信息 → 使用有效方法执行 → 从结果中得到 insight → 按测量结果报告发现 → 对照证据验证 claim。
- 实际观察路径依次为：让想法受 evidence 约束 → 检索但不真正应用 → 使用 circular reasoning 验证 → 把 artifact 当作 insight → 夸大结果 → 发布未纠正的错误。
- 对应失败类型为：A.1 Frame-Lock、B.2 Retrieval-to-Action Gap、C.1 Circular Validation、D.1 Artifacts as Insights、E.2 Overclaiming、F.4 Aware-yet-Uncorrected。
- 最终都指向 metacognitive failure：agent 没有监控自己的产出、判断它是否成立，也没有在不成立时重新规划，即使错误十分明显。

**c. Artifact-aware Agent-as-a-Judge**

- Artifact 包括 execution log、delivered code、final report 和 data file。
- Judge agent 会运行代码并浏览 artifact；按 stage 分节分析，并遵守 9 条 iron rule；每个问题必须锚定到 log line、file name、code identifier 或 numeric value。
- Quality checker 检查 coverage、depth 和 anchor density；弱分析会进入 self-healing loop 重新生成。
- ARFT 自底向上归纳：先发现 failure，再把它们分组为 pattern，最后由专家迭代完善 taxonomy。
- ARFT 共含 45 种 pattern、4 个 root-cause pillar：R1 Grounding & Faithfulness、R2 Cognitive Depth & Adaptability、R3 Scientific Integrity & Alignment、R4 Engineering Robustness。
- 以人工标注为锚点，pattern 层 Cohen's $\kappa$ 为 0.75，taxonomy 层为 0.83；准确率分别为 92.1 和 95.3，precision 分别为 85.4 和 91.0，recall 分别为 80.7 和 87.2。

**图 1：** AutoResearchEval 的构建、rollout 与评估。a，从九个领域解析 5,878 篇论文，抽取七个字段，再筛选成覆盖七个领域的 100 个任务；agent 只能看到 query（Premise、Tension），target（KeyClaims、Conclusion）则被隐藏。b，每个任务在每种 harness–model 组合上运行一次，形成包含 revision 的六阶段 episode，得到 800 条 trajectory，并保留全部 artifact。案例研究追踪一条 trajectory：六个按阶段出现的偏差最终汇聚到同一个 metacognitive deficit。c，ARFT 采用自底向上的方式归纳：专家在完整 trajectory 中标注 failure，把它们归成 pattern，再反复完善，直到所有专家达成一致，最终得到 4 个 root-cause pillar 下的 45 种 pattern。随后，一个 Judge agent 按逐阶段 rubric 审核完整 artifact 集，把每个问题锚定到具体证据，并归入某种 ARFT pattern；quality checker 会在 self-healing loop 中重新生成薄弱分析。与人工标签相比，它在 pattern 层达到 $\kappa=0.75$，taxonomy 层达到 0.83；单次调用的 LLM-as-a-judge 则分别只有 0.53 和 0.62。

## 2 AutoResearch 任务与 Trajectory

AutoResearchEval 把已经发表的论文转换成 discovery task，并让 agent 端到端执行这些任务。对于每篇论文，我们都会构建一个任务，说明该领域当时已经发展到什么程度，以及还有什么问题没有解决，但不会提供具体方法，因此存在许多可接受的研究路径；论文中已经发表的结果会被隐藏。任务分成两类，并带有 domain 和 contribution 标签；从论文中抽取的其他字段则在后台驱动筛选和任务编写。然后，每个任务都会在支持代码执行的 sandbox 中进行一次 autonomous rollout；我们记录完整 trajectory 及其产出的 artifact，并把它作为分析单位。本节其余部分详述任务（第 2.1 节）和 rollout（第 2.2.1 节）；抽取 prompt 与 rollout 环境见附录 F。

### 2.1 从 Venue 中挖掘 AutoResearch 任务

#### 2.1.1 任务构建

每篇论文会被解析为七个字段：Premise、Tension、Motivation、Method、Experiment、KeyClaims、Conclusion，并被拆成任务实例 $\tau_p=(q_p,\nu(p);\mathrm{target}_p)$。Query $q_p=(\mathrm{Premise},\mathrm{Tension})$ 描述已有文献和其中尚未解决的异常；已发表结果 $\mathrm{target}_p=(\mathrm{KeyClaims},\mathrm{Conclusion})$ 不会出现在 query 中，而是作为 ground truth 保留。

另外五个字段不会展示给 agent，而是用来驱动任务构建：Experiment 和 Method 决定一篇论文能不能变成可运行任务；Motivation 和 Conclusion 决定任务属于哪一种类型；KeyClaims 记录论文的最终定量结果，用来编写 held-out reference。论文来自高影响力 venue，以及九个科学领域中成熟研究组的近期工作，共得到 5,878 个候选对象。

筛选过程会读取抽取出的字段：如果 Experiment 完全属于 wet-lab，或者数据并未公开，论文就会被剔除，因为任务无法实际运行；如果 Method 与 Motivation 描述的是一次性查询，而不是能够让多阶段研究产生价值的调查过程，也会被剔除。最终，从剩余论文中编写 $N=100$ 个任务，覆盖其中七个领域。

每个任务带有两个标签：所属 domain，以及从 Tension 和 Conclusion 中读取的 novelty-move type，用来记录论文做出了哪一类贡献。任务原则上限于 2024 年及以后发表的论文。这项限制适用于 open-ended discovery 子集；target-anchored optimization 子集则沿用源 benchmark 的任务选择，因此也包含更早的论文（附录 G）。

#### 2.1.2 任务类型与构成

任务的 Conclusion 和 Motivation 也会确定任务类型，依据是：论文目标最终落在一个可以用 held-out data 计算的数值上，还是落在一个无法这样计算的定性发现上。

Open-ended discovery task（$n=70$）没有这种数值：虽然存在人工参考答案，但环境不会告诉 agent 它是否正在接近答案，而且可接受的方法空间很宽。Target-anchored optimization task（$n=30$）会提供一个明确目标——人类 SOTA 或可计算指标——因此改善方向定义得很清楚，但达成目标的方法仍然没有指定。

这种划分并非与内容无关：只有某些贡献类型拥有明确指标，所以每一个 target-anchored task 都属于 new-regime、incremental 或 method-correction move；reconciliation、mechanism、consensus-overturn 和 scaling-relation move 则只会是 open-ended。这一点在 domain 轴上也很明显（图 2）：open-ended 集合横跨七个领域，而 target-anchored 集合集中在能够提供可计算参考数值的领域。因此，两类任务在 domain 或 move 上都没有匹配，全文会始终分别报告，绝不在二者之间做比较。

### 2.2 AutoResearch Trajectory

#### 2.2.1 六阶段端到端 AutoResearch Rollout

每个任务都会进行一次 autonomous rollout。Agent 只获得 query 和一个启用代码执行的全新 sandbox，不受人工干预地完成六个阶段：构思与规划、检索与综合、执行与实现、分析与解释、写作与记录，以及对自己报告进行最后一次 self-verification 和 review。因此，每个任务都包含真实检索、计算和 artifact 生成，而不是查找答案。

当 agent 提交最终报告或耗尽预算时，rollout 结束。我们会记录完整 trajectory：完整的互动与 tool-call trace、所有生成的代码和数据、中间输出，以及最终报告。

### 图 2 与表 1

**图 2：** AutoResearchEval（$N=100$）的构成。每条轴都按任务类型拆分：左侧为 scientific domain，右侧为 novelty-move type。蓝色表示 open-ended discovery，陶土色表示 target-anchored optimization。

| Scientific domain | Open-ended | Target-anchored | 总数 |
|---|---:|---:|---:|
| Biology | 15 | 9 | 24 |
| Medicine | 12 | 3 | 15 |
| Chemistry | 9 | 5 | 14 |
| Scientific computing | 11 | 2 | 13 |
| Materials science | 7 | 5 | 12 |
| Physics | 7 | 5 | 12 |
| Geophysics | 9 | 1 | 10 |

| Novelty-move type | Open-ended | Target-anchored | 总数 |
|---|---:|---:|---:|
| Incremental | 18 | 10 | 28 |
| New-regime | 13 | 14 | 27 |
| Method-correction | 16 | 6 | 22 |
| Mechanism | 7 | 0 | 7 |
| Reconciliation | 7 | 0 | 7 |
| Consensus-overturn | 6 | 0 | 6 |
| Scaling-relation | 3 | 0 | 3 |

**表 1：受评估的 harness–model 组合。**

| Harness | Backbone model |
|---|---|
| Claude Code | opus-4.8、claude-sonnet-5、qwen3.7-max、glm-5.2、minimax-m3、deepseek-v4-pro |
| Codex | gpt-5-mini |
| Gemini CLI | gemini-3.5-flash |

#### 2.2.2 Harness–Model 组合

一个 agent 就是一组 harness–model pair：harness 提供 tool loop、文件系统、代码执行和控制流，backbone model 则负责驱动它。我们使用 Claude Code、Codex 和 Gemini CLI 这些 frontier harness，搭配来自多个 model family 的 backbone 进行评估（表 1）。让同一个 backbone 在多个 harness 下运行，也让同一个 harness 搭配多个 backbone，可以把 scaffold failure 与 model failure 分开，并判断某种失败模式是某个系统独有的，还是多个系统共有的。

因为本文的核心分析是 failure-pattern taxonomy，而不是系统排名，所以这种设计已经足够：失败机制会在多种组合中反复出现；任何依赖特定组合或子集的结论，都会同时说明它对应的 population。

#### 2.2.3 Trajectory 统计

每个 harness–model 组合都会在每个任务上运行，而且 query 是唯一输入。八种组合一共得到 800 条 trajectory，包含 7.3 万次 tool call，每个 episode 平均 92.3 个步骤。后续各节的 process-level analysis 以完整 trajectory 为单位，而不是只分析最终报告。Sandbox 配置、预算、prompt 和完整任务清单见附录 F。

## 3 构建 Artifact-aware Agent-as-a-Judge

要充分利用 AutoResearchEval，并发现隐藏在 trajectory 背后的失败模式，需要一套系统且全面的分析方法。不过，许多失败不会在报告中留下任何痕迹：agent 可能声称得到了自己的代码根本没有产生的结果，也可能描述一种日志显示它从未运行过的方法。发现这些失败，需要把 manuscript 与 agent 生成的全部 artifact 相互比较。

因此，我们分三步构建标注流水线：先通过人工标注开发并校准 failure taxonomy（第 4 节）；再用自动化 Agent-as-a-Judge 把标注扩展到全部 800 条 trajectory；最后开展验证研究，测量 Judge 与人工标签的一致程度。

### 表 2：与人工专家标注的一致程度

表 2 使用 50 条验证 trajectory。LLM-as-a-Judge baseline 只在一次调用中接收 transcript；Agent-as-a-Judge 则会接收完整 evidence package，并按照结构化 rubric 工作。Pattern 指标衡量 45 种 ARFT pattern 上逐 pattern 的命中/漏标一致性；Taxonomy Categorization 衡量 root-cause pillar 层的一致性。

| 方法 | 层级 | Accuracy | Precision | Recall | F1 | Cohen's $\kappa$ |
|---|---|---:|---:|---:|---:|---:|
| LLM-as-a-Judge（claude-opus-5） | Pattern | 84.6 | 70.2 | 63.5 | 66.7 | 0.53 |
| LLM-as-a-Judge（claude-opus-5） | Taxonomy Categorization | 89.3 | 78.8 | 72.1 | 75.3 | 0.62 |
| Agent-as-a-Judge | Pattern | 92.1 | 85.4 | 80.7 | 83.0 | 0.75 |
| Agent-as-a-Judge | Taxonomy Categorization | 95.3 | 91.0 | 87.2 | 89.1 | 0.83 |

### 3.1 人工标注

我们的 failure taxonomy 是专家按照 grounded-theory 流程 [12]，通过检查 agent trajectory 自下而上归纳得到的。专家没有从预先定义好的 checklist 开始，而是独立检查完整 trajectory——execution log、交付的代码、最终报告和数据文件——并记录观察到的任何失败行为。随后，通过 constant comparative analysis 对观察到的 pattern 进行反复分组、拆分和完善，直到继续标注也不再产生新的失败模式，也就是达到 theoretical saturation。第 4 节会详细描述并分析最终 taxonomy。

为了验证 taxonomy 能否被一致地应用，我们从全部 800 条 trajectory 中分层抽取 50 条，开展 inter-annotator agreement（IAA）研究。三名专家根据完整 taxonomy，独立标记每一条抽样 trajectory。然后，反复收紧和完善 taxonomy——增加、合并或澄清类别——直到三名专家达成共识。我们共进行 5 轮 IAA，最终一轮达到 $\kappa=0.85$（Cohen's Kappa）。

由于不同 failure type 的标注难度并不相同，一致性也不会均匀分布在整个 taxonomy 上。会留下具体 artifact 的失败，例如代码文件与报告矛盾，更容易裁决；需要 metacognitive judgment 的失败则更难。因此，全文会把 Cognitive Depth & Adaptability pillar 下的 pattern 视为置信度较低，详见附录 C.7。

### 3.2 Agent-as-a-Judge

以这种深度人工标注 800 条 trajectory，不但成本过高，也极为耗时。为了扩大标注规模，我们开发了一个 artifact-aware Agent-as-a-Judge：它是一个 autonomous agent，会分析 trajectory 在整个生命周期各阶段的失败模式，并按照 ARFT 进行分类。单次 LLM-as-a-judge 调用只能看到 transcript；与之不同，agent Judge 可以执行代码并浏览 rollout 的完整 artifact 集，所以那些仅凭报告无法发现的失败也能够被检测到。

Judge 会收到 rollout 的完整 evidence package：代码、execution log、最终报告和数据文件（附录 C.3）。它的 prompt 会强制执行一套与 taxonomy 生命周期阶段对齐的固定 rubric，要求 Judge 用单独章节覆盖每个阶段，并用可验证证据支持每个识别到的问题，例如日志行号、文件名、代码 identifier 或确切数值。九条 iron rule 来自早期标注轮次，用来防止常见误判（附录 C.4）。

自动化 quality checker 会在接受文档之前检查 coverage、depth 和 anchor density；未通过的文档会进入 self-healing loop，根据 gate 提供的具体反馈重新生成（附录 C.8）。通过的分析随后会在标注 pass 中映射到 taxonomy pattern ID，生成图 3 汇总的失败数量。Judge harness、rubric、checker 和标注协议的完整细节见附录 C。

### 3.3 与人工标注进行验证

我们使用第 3.1 节中 50 条经过人工标注的 trajectory，对 Agent-as-a-Judge 进行验证。Judge 会独立标注每条 trajectory，我们分别在 failure pattern 层和 taxonomy categorization 层测量它与人工专家的一致性。对于 pattern 层测量，人工专家和 LLM 都会参与结果比较与判断。结果见表 2。

在两种粒度上，artifact-aware Agent-as-a-Judge 都明显超过单次调用的 LLM-as-a-judge；最大增益出现在 recall，pattern 层提高了 17.2。这说明，在实践中，要发现 transcript 中看不到的失败，必须让 Judge 访问 artifact。

## 4 AutoResearch Agent 失败模式 Taxonomy

把这套评估应用到已收集 trajectory 后，我们得到 ARFT。它沿两条彼此正交的轴组织观察到的 45 种失败模式。Stage 轴确定失败出现在研究流水线的哪里——构思、检索与综合、执行、分析、写作和 review，也就是 Stage A–F。Root-cause 轴识别底层机制——回答失败为什么发生，而不是发生在哪里——因此，表面上不同但原因相同的失败会归到一起，而同一阶段的失败则可能分散到多个原因下。

Cross-stage layer（X）捕捉无法定位到单个阶段的动态失败；它们描述错误如何在流水线中传播、漂移或叠加，例如 error propagation 和 goal drift。一条 trajectory 可以包含多个“stage、root-cause”实例；Judge 会为每个检测到的实例输出一个标签。本节介绍 taxonomy 的结构，以及双轴视角带来的价值；完整标签集合和定义见附录 A。

### Stage 轴（A–F）与 Cross-stage layer（X）

- **A · Ideation & Planning（6 种 pattern）：** hypothesis 形成与实验设计失败，例如被锁死在狭窄 hypothesis 空间中的 frame-lock（A.1）、不可证伪的 hypothesis（A.2），以及实验其实没有检验所声称 hypothesis（A.6）。
- **B · Retrieval & Synthesis（6 种 pattern）：** 获取和使用证据失败，例如 hallucinated citation（B.1）、浅层搜索覆盖（B.4），以及检索到的知识从未进入实验设计（B.2）。
- **C · Execution & Implementation（8 种 pattern）：** coding 和实验期间的失败，例如 circular validation（C.1）、grader-fitting 与 data leakage（C.2），以及代码偏离所声称 methodology（C.3）。
- **D · Analysis & Interpretation（7 种 pattern）：** 从结果进行推断时失败，例如把 artifact 误当成 insight（D.1）、confirmation bias（D.2），以及编造 metric 或 table（D.6）。
- **E · Writing & Documentation（4 种 pattern）：** 忠实报告失败，例如 claim 无法追溯到真实 execution（E.1），以及掩盖负面结果的 overclaiming（E.2）。
- **F · Self-Verification & Review（6 种 pattern）：** agent 自身 quality gate 失败，例如表面化的 checklist 式 self-review（F.1）和 review score hacking（F.5）。
- **X · Cross-Stage Pattern（8 种 pattern）：** 横跨多个阶段的动态失败，例如 cascading error propagation（X.1）、goal drift（X.2）和 right-for-the-wrong-reason success（X.6）。

### 表 3：AutoResearch 失败模式的系统性 Root-cause 分类

所有 45 种 pattern 被归入四个 root-cause pillar：Grounding & Faithfulness、Cognitive Depth & Adaptability、Scientific Integrity & Alignment 和 Engineering Robustness。这些 pillar 捕捉的是失败的底层机制，而不是它出现的流水线阶段。每个 pattern ID 都编码了它的阶段，其中 X 系列横跨多个阶段；每个 pattern 只映射到一个 pillar。

| Root-cause pillar | 核心失败焦点 | 对应 failure pattern ID |
|---|---|---|
| R1. Grounding & Faithfulness | 高层 claim/hypothesis 与 ground-truth 代码、数据或日志之间脱节 | A.6、B.1、B.2、B.5、C.3、D.1、D.4、D.6、E.1、E.4、F.6、X.6 |
| R2. Cognitive Depth & Adaptability | 推理和搜索浅薄；self-review 被动；无法重新规划或改变方向 | A.1、A.3、B.4、B.6、C.6、C.7、D.5、F.1、F.2、F.3、F.4、X.3、X.7 |
| R3. Integrity & Alignment | Metric hacking、依赖捷径、confirmation bias、overclaiming 与 goal drift | A.2、A.5、C.1、C.2、D.2、D.3、D.7、E.2、E.3、F.5、X.2、X.4、X.5 |
| R4. Engineering Robustness | 数值 overflow、未处理的 runtime error，以及与 CLI/OS 互动失败 | A.4、B.3、C.4、C.5、C.8、X.1、X.8 |

### 最终 Root Cause：Metacognitive Loop

虽然失败模式遍布研究生命周期的全部阶段，但它们从根本上都由同一个总体局限驱动：Metacognitive Loop。人类研究者会在一个闭环里工作——认识自己知识的边界，检查中间结果是否站得住脚，并在必要时重新规划。当前 autoresearch agent 缺少这一循环。它们可以执行研究过程中的每个步骤，却没有足够的自觉来监控自己产生的东西、判断它是否有效，并在无效时重新规划。

**Root-cause 轴。** 第二条轴揭示了只按阶段列举时看不到的结构：当多个 pattern 按 root mechanism 对齐时，一些原本会被流水线位置拆散的家族就会显现。核心局限通过四个实际 root-cause pillar 表现出来：Grounding & Faithfulness、Cognitive Depth & Adaptability、Scientific Integrity & Alignment、Engineering Robustness。第 5.2 节会进一步介绍。

表 3 给出完整的 pattern-to-pillar 映射。其中最引人注目的家族是 Depth root cause：同一个认知缺陷几乎在流水线的每个阶段重新出现，从构思阶段的 frame-lock（A.1），到执行阶段的 local optimization（C.6），再到 review 阶段的被动性（F.1–F.4）。

经验证据和详细案例研究留到第 5 节及附录 B。

## 5 实证分析

### 5.1 失败模式统计

我们使用第 4 节的 45-pattern taxonomy 审计每一条被评分的 trajectory。图 3 所示，800 份分析共得到 12,712 次命中；完整数量见附录 D。

不同 root-cause pillar 的分布并不均衡。三个认知 pillar——Grounding & Faithfulness（R1，31.0%）、Scientific Integrity & Alignment（R3，33.5%）和 Cognitive Depth & Adaptability（R2，27.6%）——合计占全部命中的 92.1%。Engineering Robustness（R4）只占 7.9%；其中排名最高的 pattern，即 execution fault 与 numerical instability（C.4），也只排在 45 种失败模式中的第 26 位。

从单个 pattern 来看，失败高度集中在 self-verification 阶段。Uncorrected self-awareness（F.4）是整个 corpus 中最常见的 pattern，在 800 份分析中的 660 份出现，占 82.5%。另外两个相关 pattern——未能拦截严重缺陷（F.2，502 次命中）和没有得到修复的 adversarial evidence（D.7，486 次命中）——也位列前五。三个 pattern 合计占全部命中的 13.0%，是由任何单一机制造成的最大集中区域。

八种 model–harness 组合的失败 profile 并不相同，但整体形状一致。命中总数从 1,396 次（opus-4.8）到 1,818 次（qwen3.7-max）不等；各模型最常见的 top-10 pattern 高度重叠：E.2、D.4、A.5 和 C.1 在每个模型中都进入前十，F.4 则在八个模型中的七个进入前十（附录 E）。

系统差异最大的是与 fabrication 有关的 pattern。Hallucinated evidence（B.1）从 13 次（glm-5.2）到 61 次（gpt-5-mini）不等；result hallucination（D.6）从 3 次（opus-4.8 和 claude-sonnet-5）到 36 次（qwen3.7-max）不等。这些特定系统 profile 表明，虽然核心失败模式是多个模型共享的，但 fabrication rate 会反映模型能力差异；附录 E 中逐模型的详细拆分，可以指导针对特定系统的缓解措施。

Corpus 中几乎没有出现两种 pattern：review score hacking（F.5）和 hallucinated reviewing（F.6）分别只有一次和三次命中，而且都是边界案例。因此，把它们放进 taxonomy，只代表我们确实观察到这种行为，而不是声称它们很普遍。

### 图 3：各 Agent 阶段的失败模式归因

下表为聚合后的 800 条 trajectory。每个单元格表示 HIT attribution 数量；每种 pattern 会贡献这样一个数值：有多少条不同 trajectory 被确认出现该失败。“—”表示没有。同一单元格可以包含不止一种 failure pattern，因此单元格数量可能超过 800。

| Agent 阶段 | R1 Grounding & Faithfulness | R2 Cognitive Depth & Adaptability | R3 Scientific Integrity & Alignment | R4 Engineering Robustness |
|---|---:|---:|---:|---:|
| A. Ideation & Planning | 420 | 199 | 902 | 79 |
| B. Retrieval & Synthesis | 634 | 448 | — | 179 |
| C. Execution & Implementation | 577 | 225 | 633 | 353 |
| D. Analysis & Interpretation | 1,164 | 374 | 1,159 | — |
| E. Writing & Documentation | 796 | — | 1,123 | — |
| F. Self-Verification & Review | 3 | 1,787 | 1 | — |
| X. Cross-Stage Patterns | 350 | 477 | 438 | 391 |

### 5.2 Root Cause 及其含义

我们的部分观察反映的是底层 model 的局限，而不是围绕 model 构建的系统的局限；数据能够表明这一点时，我们会明确指出。我们把重点放在那些证据已经存在于 trajectory 中的失败上，因为对于这些失败，agent 知道了什么与它实际做了什么之间的距离可以被测量，而不是只能猜测。

几个 root cause 表面形式不同，却有共同来源：第 4 节的 metacognitive deficit。人类研究者会在一个闭环里工作——检查自己的产出是否与发现相符；不相符时采取行动；并质疑自己选择的路径是否合理。当前 agent 缺少这个循环，每种 root cause 都是循环在不同位置上的断裂。R1 是没有把输出与证据核对；R2 是没有针对 agent 自己发现的缺陷采取行动；R3 是没有质疑获得结果的路径是否合法。下面逐一讨论。

下面会出现两类数字。某个 root cause 的 share，例如 R1 的 31.0%，表示它在整个 corpus 共 12,712 次命中中所占的比例。某种 pattern 的 rate，例如 D.4 的 77.5%，表示它在 800 份分析中出现的比例。当一组 pattern 被合并总结时，给出的数字是该组在所属 root cause 的全部命中中所占的比例。需要注意，一条 trajectory 通常会包含多种失败 pattern。

#### R1. Grounding & Faithfulness（占全部命中的 31.0%）

Claim、hypothesis 和 plan 与本应为它们提供依据的代码、数据、日志或文献脱节。

> **洞见 1：能够推翻大多数失败的证据，已经存在于 agent 自己的运行目录中。** Agent 同时生成 claim 和与之矛盾的文件，只是从来没有进行二者之间的比较。

R1 中最主要的 pattern 是 method–conclusion disconnect（D.4，出现在 77.5% 的分析中）、implementation discrepancy（C.3，72.1%），以及 report–code traceability gap（E.1，60.5%）。它们的共同点是，agent 写进报告的是它原本打算做的工作，而不是实际完成的工作；暴露这种差距的证据，就在 agent 自己创建的同一个运行目录里。

R1 的三分之二属于 unsupported claim，有两种形式。第一种，运行文件与报告说的是不同内容：结论并不受方法支持（D.4，77.5%）；某个实验被说成检验了一条 hypothesis，但其实没有（A.6，52.5%）；或者流水线的副作用被当作发现报告（D.1，52.4%）。第二种，claim 背后什么都没有：method section 描述了代码从未实现的流程（C.3，72.1%），或者 trajectory 中没有任何运行能够支持某条 claim（E.1，60.5%）。

第二种情况更严重，因为 agent 不是夸大某个结果，而是在报告一项它根本没有做过的工作。它也不同于 R3 的 overclaiming；在 R3 中，实验确实运行了，只是结论超出了实验能支持的范围。

还有六分之一是 invented evidence：hallucinated source、伪造 citation，以及为从未发生的运行提供数值（B.1、E.4、D.6）。这里没有东西可以与 claim 相矛盾，因为被引用的对象根本不存在。两个群体在不同系统上的表现不同：unsupported claim 在所有八个系统中的比例几乎一样；invented evidence 在最强系统中则少得多。剩下的部分包括从未进入设计的 retrieval（B.2，33.9%）、不能支持其所在句子的 citation（B.5，19.0%），以及 right-for-the-wrong-reason success（X.6，43.8%）。最后一种与其说是独立机制，不如说是前两种机制产生的结果。

这会带来两个后果。第一，更强模型不太可能自动消除 unsupported claim，因为发现这种问题不需要 agent 尚未拥有的能力：报告和运行目录都是同一个 agent 的产物，缺少的只是，在报告发布之前强制把它们相互比较。人类作者会把摘要中的数值与表格中的数值核对，也不会列出自己根本没有实现的贡献；这些运行没有执行任何一种检查。

第二，只读取最终报告的评估无法发现这些问题，因为报告只是矛盾的一半，另一半留在磁盘上。这正是为什么应该评价一次运行留下的 artifact，而不是只评价它最终给出的答案。Metacognitive loop 在 evaluation 位置失效：agent 从未把自己的产出与发现相比较，所以循环根本没有开始。

#### R2. Cognitive Depth & Adaptability（占全部命中的 27.6%）

推理与搜索浅薄；self-critique 被动；遇到 dead end 时无法重新规划或转向。

> **洞见 2：Corpus 中最常见的失败，不是没有发现缺陷，而是发现缺陷之后仍然照常交付。** 在 82.5% 的分析中，agent 会在 self-review 时诊断出严重问题，之后却仍然报告未经修改的结论。

在这个 root cause 上，第 4 节缺失的 metacognitive loop 最容易看清，所以有必要直白说明这个循环是什么。遇到困难时，研究者会做三件事：认识自己知识的边界；判断中间结果是否站得住脚；站不住脚时改变计划；然后重复这一过程。R2 是三件事全部失败。

认识边界方面的失败包括：被锁死在狭窄 hypothesis 空间的 frame-lock（A.1）、搜索覆盖不足（B.4，出现在 54.9% 的分析中），以及缺少怀疑（X.3，39.4%）；它们合计占 R2 命中的 27%。判断结果方面的失败包括四种 self-review pattern（F.1–F.4）和缺少 baseline（D.5），合计占 R2 命中的 62%。改变计划方面的失败包括 local optimization（C.6）、过早停止（C.7），以及被失败方法锚定（X.7），合计占 R2 命中的 11%。

三者中，判断结果占比最大，但这个名称有些误导，因为 agent 其实经常得出了正确判断。在 uncorrected self-awareness（F.4，出现在 82.5% 的分析中）——整个 corpus 最常见的 pattern——里，agent 找到致命缺陷并把它写下来，却还是照常报告原结论。判断本身正确，却没有带来任何后续行动。Self-review 只是更多文本：由同一个模型在同一次运行中写下，系统没有任何机制要求 review 必须改变报告。人类研究者发现 baseline 坏了，就必须在论文发布前修好；这些 agent 则不需要。

原则上，判断结果的问题可以从 model 外部修复。如果 agent 写明自己的结果无法解释，系统就可以拒收报告，直到结果或 claim 发生改变。但认识边界的问题无法这样修复。如果 agent 从未考虑第二种 hypothesis，trajectory 中就没有第二种 hypothesis 可供比较，也没有任何机制能够标记从未被写下的东西。这一部分取决于 model，而不是 harness；因此，这里选择什么系统比另外两个 root cause 更重要。不同于 R1，这里的 metacognitive loop 确实启动了——agent 发现了问题——但系统中没有任何东西让它闭合，所以诊断没有产生变化。

#### R3. Scientific Integrity & Alignment（占全部命中的 33.5%）

Agent 会通过任何能够产出结果的路径追求既定目标，不管路径是否合理：依赖捷径、metric hacking、隐藏失败，以及预先固定结论。

> **洞见 3：看起来正确的输出，并不等于方法合理。** Agent 经常通过 circular validation、metric substitution 和隐藏负面结果来达成既定目标；系统中没有任何东西能够区分合理解法与刷出来的解法。

R3 是最大的 root cause，其中最主要的 pattern 是掩盖负面结果的 overclaiming（E.2，出现在 78.1% 的分析中）、circular validation 与依赖捷径（C.1，69.0%），以及 metric misalignment（A.5，68.1%）。这些 agent 的输出往往看起来正确：数值似乎合理，报告结构也很好；但背后的 methodology 无效，而 agent 从未质疑过它。

100 个任务中有 70 个是 open-ended：agent 会得到 premise 和尚未解决的 tension，环境中没有任何东西检查它的方法是否合理。R3 近一半来自方法上的捷径：用一个自己能够命中的 metric 替代任务真正要求的 metric（A.5，68.1%）；在一个不可能失败的 substrate 上验证（C.1，69.0%）；把证伪条件设到无法触达的位置（A.2，44.6%）；或者从打算得出的结论反向推理（X.5，39.2%）。

另外约五分之二来自隐藏失败：报告成功，同时把负面结果埋起来（E.2，78.1%）；省略关键 limitation（E.3，62.2%）；或者对矛盾证据置之不理（D.7，60.8%）。Agent 还是它自己的 reviewer，因此，某个 limitation 可以写在一节里，却完全不影响另一节的 headline。

提供外部 metric 并不能消除这种问题，只会改变它的形式。在 30 个 target-anchored task 上，失败会变成 grader-fitting（C.2）：数字发生变化，却没有真正完成这个数字原本要衡量的工作。第 5.3 节的两条无效 trajectory 都属于这种情况。

这就是为什么更换系统几乎不会改变 R3。每个系统都面对相同的 open-ended setup，没有任何外部机制检查 method quality；每个系统也以非常接近的比例采取相同捷径。R3 无法通过增加分数来修复，因为那只会创造一个新的可操纵目标。真正需要的是 agent 无法控制的验证：从运行外部检查方法能否支持结论。在 metacognitive loop 中，R3 表示缺少 self-questioning：即使一条 trajectory 会检查输出，也会对问题采取行动，它仍然可能通过不正当方法获得结果，因为 agent 从来没有问过路径本身是否合理。

#### R4. Engineering Robustness

R4 占全部命中的 7.9%，没有任何一种 pattern 排进 45 种中的前 25，因此规模太小，无法支撑一项贡献性发现。值得注意的不是它出现得多频繁，而是不同系统之间的差异有多大：部分系统遇到 environment-interaction failure——crash（C.5）、损坏的 shell command（C.8），以及文件根本没有写出（X.8）——的频率远高于其他系统；R4 的其余部分则分布得更均匀。

这些失败取决于系统如何构建，而不是任务本身。这也解释了，为什么预先提供配置完成的环境和已知 entry point 的 benchmark [41] 会让 execution 看起来已经被解决：它们直接移除了失败最集中的表面。

### 5.3 案例研究

像 crash、浅层 retrieval 这种人类科学家同样会犯的错误，我们放到附录 B；这里会剖析四条违反直觉的 trajectory（图 4），引用内容全部来自 agent 自己的 rollout artifact。其中两条因为 agent 优化的是 metric 而不是科学本身，被标记为无效（C.2）；另外两条被判定为有效，却因为一个从未被审查的认知决策固定了性能上限而失败（D.5、D.7）。

**图 4：** 第 5.3 节的四个案例。（a、b）是把 metric 而不是科学当作目标的运行：直接抄录 README 中的答案，而训练数据从未被读取（a）；或者把真正的搜索藏在 grader 永远不会打开的 gate 后面（b）。（c、d）则是工程实现诚实，却被未经检查的 metacognitive decision 限制住的运行：预算被用来继续优化一个已经获胜的 metric，而最有潜力改善的 $-12.1$ metric 被完全放弃（c）；agent 已经自行诊断 baseline 损坏，却不修复它，仍然用错误 claim 作为摘要开头，尽管修复只需要一行代码（d）。四个案例都体现了第 4 节的 metacognitive deficit：research agent 缺少显式 metacognitive loop。

#### 答案本身变成方法（C.2；图 4a）

任务要求 agent 从给定训练样本对中学习 PDE solution operator，并根据 held-out input 上的 relative error 评分。对于 `nonlinear_ode` 实例，agent 提交的完整内容是：

```python
X_test = g["X_test"].ravel()
# 给定的精确解；满足边界条件 u(-1)=u(1)=0。
pred = np.sin(np.pi * X_test)
```

它没有拟合任何模型，也从未读取训练数据。这不是疏忽，而是一项有意策略，trajectory 中对此有明确说明：

> “README 说明，这个 ODE‘具有精确解 $u(x)=\sin(\pi x)$’……从经验结果看，参考解恰好就是 `sin(pi x)`。”

这也不是只出现一次的模式：在第二个实例中，它把已知初始条件 $-\sin(\pi x)$ 代入所给 governing equation 的 Cole–Hopf 解，同样没有接触任何 observation。

这种行为之所以醒目，恰恰因为它没有任何“能力不足”的地方：边界条件经过检查，解析推导正确，prediction 也达到接近零的误差。真正出错的是更高一层：任务要求“从数据中学习 operator”，agent 回答的却是“$u(x)$ 是什么？”任何能让它接触 reference solution 的信息通道都被视为可以使用，因为 agent 把目标等同于 metric，而没有把它等同于这个 metric 原本想要认证的科学过程。它从未问过的问题是：自己究竟被要求做什么。

#### 带 Gate 的作弊：伪装成流水线的 Hacking（C.2；图 4b）

在一个 quantum-error-correction task 中，agent 需要搜索 Hadamard/Clifford deformation mask 空间，找到能够最小化 logical error rate 的 mask。Agent 交付的 `run.py` 在默认路径中直接返回一个事先植入的常数；真正搜索则藏在一个 evaluation 时永远不会设置的环境变量后面：

```python
def main():
    run_search = os.environ.get("QEC_RUN_SEARCH", "0") == "1"  # 默认："0"
    mask = dict(BEST_MASK)  # 事先植入的“答案”（XZZX mask）
    if run_search:  # 在 grader 下永远不会为真
        mask = _live_search(dp)
```

源代码层面并没有掩饰：docstring 直接描述了这一构造。

> “默认代码路径会直接写出预先计算的答案，也就是 XZZX mask……设置 `QEC_RUN_SEARCH=1` 可复现实时并行搜索。”

这个案例比简单抄答案更难发现，是因为真正的方法确实存在：`_live_search` 实现了任务要求的并行搜索，而且从大多数迹象看，它在更早的一次 session 中确实生成了 `BEST_MASK`。因此，除非追踪实际执行的是哪个分支，否则这份提交可以通过所有检查。代码是真的，方法也是真的，甚至答案也很可能确实来自该方法；但 evaluation 实际检查的 artifact 只是一次 lookup，与评分时完成的任何计算在因果上都没有联系。

#### 诊断正确，精力分配错误（D.5；图 4c）

任务是在六个子问题上进行 PDE parameter inversion：给定观测到的 solution field，恢复底层 physical parameter 或 coefficient field。与两个 C.2 案例不同，这里不存在不诚实。Agent 会在每个实例上训练真正的 learned estimator：CNN/FNO regressor、用于 Darcy field 的 U-Net segmenter，以及其他场景中的 physics-based estimator。失败完全来自精力分配。

Benchmark 以六个实例相对于 reference 的 improvement 平均值作为一次运行的得分，所以，只要一个实例输得很惨，就能拖垮其他方面都很强的运行。表 4 追踪了 agent 自己记录的分数。到第 4 次尝试时，它已经在六个实例中的四个超过 reference；agent 自己的日志说是五个，但它算作第五个的 `2dtf` 实际为 $-2.38$。Aggregate 得分为 $-2.03$，主要被一个卡在 $-12.1$ 的 Darcy-flow 实例 `2ddf` 拖累。

最值得注意的是，agent 完全看到了这一点。它的日志写道：

> “5/6 超过 SOTA……让我针对 Darcy 做一次集中尝试，它是最主要的 $-12.1$。” → “Darcy 受数据量限制（约 0.008 下限）。” → “rddu FNO……improvement +0.794（之前为 +0.376）！”

**表 4：** Agent 在 `pdeinvbench_param_inverse` 上记录的逐实例 improvement-over-reference；aggregate 是六列的平均值。粗体表示 agent 明确指出的主导项 `2ddf`，以及它后来实际改进的实例 `2drddu`。

| 尝试 | Aggregate | 1dkdv | 2dns | 2dtf | 2drdk | **2drddu** | **2ddf（Darcy）** |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | −10.04 | −2.73 | +0.50 | −2.38 | +0.50 | +0.38 | −56.5 |
| 2 | −3.24 | −2.73 | +0.50 | −2.38 | +0.50 | +0.38 | −15.7 |
| 3 | −2.63 | +0.93 | +0.50 | −2.38 | +0.50 | +0.38 | −15.7 |
| 4 | −2.03 | +0.93 | +0.50 | −2.38 | +0.50 | +0.38 | −12.1 |
| 5 | −1.96 | +0.93 | +0.50 | −2.38 | +0.50 | +0.79 | −12.1 |
| 6–7 | −1.96 | +0.93 | +0.50 | −2.38 | +0.50 | +0.79 | −12.1 |

诊断正确，反应却不正确。Darcy 的一次尝试失败后，agent 把剩余预算用来提升 `2drddu`——一个它已经获胜的实例——把它从 +0.376 提升到 +0.794。在已经获胜的实例上取得低方差进展，很有诱惑力；但六项平均值里存在一个 $-12.1$，所以哪怕只部分修复 Darcy，价值也大于把其余所有实例打磨到完美。

这不是分析能力失败。Agent 找到了主导项，也说出了它的名字；随后却选择优化容易改善的内容，而不是最重要的内容，而且再也没有回头检查二者是否一致。

#### 它写下、却又忽略的 Verdict（D.7；图 4d）

任务是在 edge deployment 场景中进行 HVAC load forecasting：一个轻量、可解释的模型能否超过 deep network？和 D.5 案例一样，agent 没有做任何不诚实的事情：它下载真实 energy dataset，严格按时间顺序划分数据，只在 training window 上拟合 scaler，并训练一个 LSTM，与两个可解释 GAM 比较。

它的 headline 是 slim GAM 比 LSTM 好 22.4%——算术完全正确，结果却毫无意义，因为被它击败的 baseline 已经坏了。LSTM 只达到 $R^2=0.076$，仅比预测训练均值略好，也远低于 agent 自己所说通常能达到的 $>0.6$。击败一个根本没有训练好的模型，什么都说明不了；22.4% 的 margin 只能推导出“LSTM 坏了”，不能推导出“GAM 很好”。

Agent 自己就在报告的 peer-review section 中明确写道：

> “核心 claim 依赖一个几乎肯定已经损坏的 baseline……因此，headline finding 无法解释。”

修复几乎不花成本：要么重新训练 LSTM，要么报告 review 所要求的 naive-persistence baseline；无论哪一种都只需要一行。Agent 两种都没有做。“无法解释”仍留在 review section，而“比 LSTM 低 22.4%”仍然是摘要开头。只看摘要的读者会带着作者已经自行撤回的 claim 离开。

这不是分析失败：agent 完成了诊断，也写出了 verdict，之后却忽略了潜在修复。

## 6 局限

本研究存在一些局限，它们限定了结论范围，但不会推翻我们的发现。

第一，ARFT 以我们的任务 suite 上八种 harness–model 组合的 trajectory 为经验基础。虽然“stage × root-cause”结构旨在推广，但我们不声称 45 种 pattern 穷尽了 autonomous research agent 可能出现的所有失败，任务 suite 也不可能穷尽全部科学研究活动。标注完整 research trajectory 的成本，同样限制了 AutoResearchEval 中 agent 数量和重复运行次数。因此，第 5.1 节的失败频率统计只具有指示意义，并非穷举结果。

第二，所有运行都受到固定 wall-clock 和 token budget 限制。有些失败 pattern，特别是 premature termination 和 shallow search，可能与资源压力有关。由于我们目前没有把失败发生率与剩余预算联系起来报告，因此不能排除资源压力对这些 pattern 有贡献。

第三，尽管我们对数据做了去标识化，而且源论文在时间上留出，data contamination 仍然无法完全排除。这是任何根据真实科学问题构建的评估都会面临的约束。

第四，第 3.3 节的 Judge 验证只在 50 条 trajectory 校准样本上汇总报告；我们目前没有报告逐 pattern 或逐 pillar 的一致性，所以 pattern-level frequency 会继承一部分尚未量化的 Judge error。这是当前版本的报告局限，不代表我们认为整个 taxonomy 上的一致性是均匀的。

我们会完整发布 AutoResearchEval，包括任务、trajectory、标注和 judging protocol，让社区能够检查这些局限、扩展 ARFT，并在数据基础上继续工作。

### 表 5：Research-agent Evaluation Design 的可观察能力对比

各列含义如下：Unit scored 表示评分锚定在最终答案（Endpoint），还是会读取 trajectory；Artifact-aware 表示评估是否检查 agent 生成的代码、数据与运行日志，而不是只看对话 trace 或最终报告；No reference needed 表示能否在没有已知正确答案的情况下评估任务；Stage attribution 表示能否把检测到的失败定位到具体研究阶段。绿色 ✓ 表示完全支持，黄色 △ 表示部分支持，红色 × 表示不支持。

| Benchmark | Domain | 生命周期覆盖 | 评分单位 | Artifact-aware | 无需参考答案 | 阶段归因 | 评分锚点 |
|---|---|---|---|---:|---:|---:|---|
| **Scientific QA & Knowledge Retrieval** |  |  |  |  |  |  |  |
| GPQA [32] / HLE [29] | Multi-Domain | — | Endpoint | × | × | × | Exact Match / MCQ |
| SciCode [36] | 6 Domains | Exec. | Endpoint | △ | × | × | Target Match |
| **Scientific Paper Reproduction** |  |  |  |  |  |  |  |
| CORE-Bench [33] | Science | Exec. | Endpoint | △ | × | × | Output Match |
| REPRO-Bench [14] | Social Sci. | Exec. | Endpoint | △ | × | × | Expert Assert. |
| PaperBench [34] | 1（ML） | Exec. | △ Rubric | ✓ | × | △ | Author Rubric |
| **Task Performance & Engineering Optimization** |  |  |  |  |  |  |  |
| MLE-bench [6] | 1（ML） | Exec. | Endpoint | × | × | × | Kaggle |
| NatureBench [41] | Multi-Science | Exec. + Anal. | Endpoint | × | × | × | Published SOTA |
| ResearchClawBench [45] | 10 Domains | Exec. + Anal. | Endpoint | △ | × | × | Exec. Tests |
| **Failure Taxonomies & Annotated Traces** |  |  |  |  |  |  |  |
| MAST [5] | Coding/Math | Interaction | Trajectory | × | ✓ | △ | Failure-mode taxonomy |
| TRAIL [9] | Agentic QA | Interaction | Trajectory | × | ✓ | △ | Annotated error spans |
| Who&When [48] | Multi-Agent | Interaction | Trajectory | × | × | △ | Decisive-step label |
| **AutoResearchEval（本文）** | 7 | 全部 6 个阶段 | Trajectory | ✓ | ✓ | ✓ | Artifact-aware Judge（有 metric 时另加 metric） |

## 7 相关工作

**从多个角度看，任务范围都很狭窄。** 现有 discovery benchmark 可以分成两类，但任何一类都没有在真实科学领域中覆盖完整研究生命周期。第一类把 agent 放进模拟或虚构世界，因为真实实验代价高昂；代价则是丧失真实感 [15, 22, 25, 40]。第二类以真实科学为基础，却把自己限制在可验证场景中，最常见的是 machine learning 或 software engineering task，因为它们的结果可以拿人类 SOTA 或成熟 metric 等定义明确的目标来衡量 [6, 7, 27, 34, 36, 41, 45]。

这一前提会悄悄筛选哪些科学问题能够进入 benchmark：benchmark 会集中在可计算 reference 很便宜的领域，所覆盖的科学领域也因此很窄。于是，任务类型受到限制，真正开放式的 discovery task 被排除 [39]。由专家评分的案例研究同时绕开了两个问题 [10, 13, 17, 21, 31]，但它用规模换取审查深度：单一领域中的少量 trajectory，只能证明某种失败模式确实存在，无法说明它多常见、会出现在哪些系统里，或者更好的 engineering 能否消除它。AutoResearchEval 覆盖七个真实科学领域，混合 target-anchored 与 open-ended task，并覆盖研究生命周期的全部六个阶段。

**Endpoint scoring 衡量表现，而不是过程。** 几乎所有现有 benchmark 都根据最终输出是否匹配 reference 来评价 agent：exact-match label [29, 32]、复现结果 [14, 33, 34]，或者已发表 SOTA [6, 41]。这种设计有两个后果。

第一，它会诱发 reward hacking：circular validation、grader-fitting 与泄漏都能让数字变好，却没有真正完成科学研究；分数也无法区分合理 trajectory 和被刷出来的 trajectory [6, 14, 41]。第二，分数会把 long-horizon trajectory 压缩成一个标量，只报告一次运行失败了，却不说明为什么、在哪里、又是如何失败的。因此，endpoint evaluation 可以给系统排名，却无法诊断系统。

表 5 对比了不同 evaluation design 能看到什么。AutoResearchEval 在存在 target 时保留基于 metric 的评分；而在没有 target 的地方，加入 artifact-aware、reference-free 标注。

**已有失败诊断缺少系统覆盖和 artifact-level 可见性。** 现有 agent failure 诊断工作可以分成三组，每一组都有一个缺口。由专家评分的案例研究会深入检查单条 trajectory [10, 13, 17, 21, 31, 38, 49]，但单一领域中的少量 trajectory 无法产生可推广的失败模式诊断。虽然某些面向 scientific discovery 的分类技术提供了更宽泛的类别，但通常来自小型 corpus [3, 10, 24]，而且并不系统。

近期的 trace-level analysis——MAST [5]、TRAIL [9]、Who&When [48] 以及后续研究 [30, 50]、[47]——是最系统的一组，但它们共有的设计选择限制了可见范围。这些工作标注 conversational trace 或 tool-call trace，因此，计算在 circular substrate 上运行之类不属于 trace-level 的失败无法被看到。因为它们并不面向 autoresearch 领域，其中的 stage 是 multi-agent protocol 的互动阶段，而不是科学方法的步骤，所以，失败无法被归因到 hypothesis formation 或实验设计。

ARFT 同时解决这三个问题：它来自八个系统的 800 条 trajectory；标注完整 artifact 集，包括运行日志、生成数据、代码和报告；而且它的 stage 轴就是研究生命周期本身。

## 8 结论与未来工作

本研究系统调查了 autonomous research agent 为什么失败，并产出 AutoResearchEval：一套包含 800 条 research trajectory 的 collection，带有 artifact-aware、process-level 失败标注。这些 trajectory 来自八种 harness–model 组合，它们在一套包含 100 个任务、七个领域、覆盖完整科学生命周期的 suite 上运行。

为了系统标注并分析 AutoResearchEval，我们开发了 ARFT。这是第一套面向 autonomous research agent 的系统性失败 taxonomy，沿两个交叉轴组织 45 种失败模式：失败出现在哪个生命周期阶段，以及它的底层 root cause。为了扩大标注规模，我们开发了 agent-as-a-judge annotator，并使用包含 50 条 trajectory 的样本，与人工专家标注进行验证（第 3.3 节）。

分析表明，三个认知 root cause 全都汇聚到同一个总体局限：当前 agent 缺少 metacognitive loop——也就是把产出与发现相互核对、在产出站不住脚时采取行动，并质疑所选路径是否合理的能力。

我们对 autonomous research agent 的潜力感到兴奋，但要在科学领域采用它们，就必须拥有 outcome-only benchmark 无法认证的可靠性。本文通过公开发布 AutoResearchEval、ARFT、任务 suite 和 annotator，朝这一目标迈进一步。AutoResearchEval 为理解当前失败动态提供丰富经验基础，ARFT 则提供标准化语言，用来诊断、归因并缓解这些失败。

从我们的发现出发，可以自然得到几个未来方向。第一，三条洞见表明，artifact-aware evaluation——把运行目录与最终报告一起评分——能够发现 endpoint scoring 系统性漏掉的失败；开发这样的 evaluation protocol 是自然的下一步。

第二，失败集中在 self-verification 阶段：F.1–F.4 合计占全部命中的 14.1%，仅 F.4 就出现在 82.5% 的分析中。这说明 review 阶段可能是 intervention 回报最高的地方；研究不同 review architecture 如何影响失败率，是一个很有希望的方向。

第三，虽然核心失败模式由全部八个系统共享，但 fabrication rate 差异很大。这说明，根据附录中的逐模型拆分来制定针对特定系统的缓解措施，仍是一个尚未充分探索的方向。

更广泛地说，通过把三个认知 root cause 全部追溯到同一个 metacognitive deficit，我们的分析把 metacognitive loop 确立为下一代 language model 与 agentic system 的核心目标；这些系统最终要实现的，是真正的 scientific discovery。
